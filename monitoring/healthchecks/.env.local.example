# Healthchecks — local
#
#   cp .env.local.example .env.local

# --- Images ------------------------------------------------------- [image] --
APP_TAG=v4.2

# --- App Configuration ----------------------------------------------- [app] --
# Django SECRET_KEY:  openssl rand -base64 60 | tr -d '\n='
HC_SECRET_KEY=__REPLACE_ME__

# --- Timezone -------------------------------------------------------- [app] --
TZ=UTC
