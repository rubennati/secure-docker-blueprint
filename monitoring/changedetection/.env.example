# =============================================
# changedetection.io – Environment
# =============================================
# Copy this file to .env and adjust all values.
# NEVER commit the .env file.
# =============================================

COMPOSE_PROJECT_NAME=changedetection

# --- Domain & Traefik ---
APP_TRAEFIK_HOST=changes.example.com
APP_TRAEFIK_CERT_RESOLVER=cloudflare-dns
APP_TRAEFIK_TLS_OPTION=tls-aplus
# Contains URLs + scraping rules — VPN-only default
APP_TRAEFIK_ACCESS=acc-tailscale
APP_TRAEFIK_SECURITY=sec-3
TRAEFIK_NETWORK=proxy-public

# --- Images ---
# dgtlmoon/changedetection.io
APP_TAG=0.60.3

# --- Containers ---
CONTAINER_NAME_APP=${COMPOSE_PROJECT_NAME}-app

# --- Timezone ---
# Let watches target private and reserved addresses (the Docker network, the
# LAN). Off by default; the compose file says what turning it on opens.
CD_ALLOW_PRIVATE_ADDRESSES=false
TZ=UTC
