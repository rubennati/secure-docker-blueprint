# =============================================
# Beszel – Environment
# =============================================
# Copy this file to .env and adjust all values.
# NEVER commit the .env file.
# =============================================

COMPOSE_PROJECT_NAME=beszel

# --- Domain & Traefik (hub UI) ---
APP_TRAEFIK_HOST=beszel.example.com
APP_TRAEFIK_CERT_RESOLVER=cloudflare-dns
APP_TRAEFIK_TLS_OPTION=tls-aplus
# Host metrics expose system details — VPN-only default.
APP_TRAEFIK_ACCESS=acc-tailscale
APP_TRAEFIK_SECURITY=sec-3
APP_INTERNAL_PORT=8090
TRAEFIK_NETWORK=proxy-public

# --- Images ---
# henrygd/beszel (https://github.com/henrygd/beszel)
APP_TAG=0

# --- Containers ---
CONTAINER_NAME_HUB=${COMPOSE_PROJECT_NAME}-hub
CONTAINER_NAME_AGENT=${COMPOSE_PROJECT_NAME}-agent

# --- Agent key ---
# Leave empty on first start. After the hub is up:
#   1. Open the UI, create the owner account
#   2. Settings → "Add agent" → copy the SSH public key shown
#   3. Paste here, then `docker compose up -d` to restart the agent
AGENT_KEY=

# --- Extra filesystems (optional) ---
# Comma-separated list of mount points the agent should report on
# (besides the root FS). Example: /mnt/nas,/var/lib/docker
EXTRA_FILESYSTEMS=

# --- Timezone ---
TZ=UTC
