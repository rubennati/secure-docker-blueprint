# =============================================
# CrowdSec – Log Acquisition
# =============================================
# Defines which log sources CrowdSec monitors.
# Traefik must write access logs in JSON format.
# =============================================

# Traefik access logs (JSON format)
filenames:
  - /var/log/traefik/access.log
labels:
  type: traefik
