---
title: Hardened Docker stacks, and the guides to operate them
description: Security-focused Docker Compose infrastructure with Traefik, CrowdSec, Docker Secrets and operational guidance for self-hosted services.
template: splash
hero:
  # The brand lives in the header wordmark. This line does the other job —
  # saying what the site is for. Putting the name here as well would repeat it
  # sixty pixels apart and leave the largest text on the page saying nothing.
  title: Running the service is the easy part. Operating it is the challenge.
  tagline: Hardened Docker Compose blueprints for reverse proxy, secrets, and intrusion detection — set up once, reused by every service you self-host.
  actions:
    - text: Getting Started
      link: /getting-started/
      icon: open-book
    - text: Applications
      link: /applications/
      icon: right-arrow
    - text: GitHub Repository
      link: https://github.com/rubennati/secure-docker-blueprint
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

Starting a container is one command. Keeping the service reachable only to the
people who should reach it, patched, and recoverable when the disk goes — that
is what these guides are about.

## What the guides cover

<CardGrid>
  <Card title="Infrastructure" icon="setting">
    Traefik terminates TLS and routes every service; nothing else publishes a
    port. CrowdSec is the optional layer that watches and blocks.

    [What every server needs →](/infrastructure/)
  </Card>
  <Card title="Applications" icon="puzzle">
    Passwords, files and calendars, invoicing, file sync, document editing —
    each with installation, verification, backup and updates.

    [What each one is for →](/applications/)
  </Card>
  <Card title="Operations and recovery" icon="approve-check">
    Backups with Borgmatic and the restore that proves them, plus a method for
    finding which layer is actually broken.

    [What running this involves →](/operations/)
  </Card>
  <Card title="Security" icon="open-book">
    Firewalls, TLS, credentials, isolation, detection, recovery — what each
    measure stops, what it does not, and where this blueprint sits in the chain.

    [The whole chain →](/security/)
  </Card>
</CardGrid>

Also here: [what self-hosting does not answer](/sovereignty/) — who governs each
piece of software, and what still leaves the server anyway.

## What is claimed about testing

Each guide opens with the version it is written against and, where one exists,
the date it was verified on a real host — followed by one line naming what was
*not* exercised. One application has had its data restored from a backup. The
others say so instead of implying otherwise.

[How to read that line →](/faq/)
