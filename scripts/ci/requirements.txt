# Python dependencies for CI scripts — pinned with hashes for Scorecard compliance.
# PyYAML has no transitive dependencies; only its own hashes are required.
#
# Hashes cover the wheels pip will select on supported runner configurations:
#   cp312  manylinux x86_64  — ubuntu-24.04 (current ubuntu-latest)
#   cp313  manylinux x86_64  — next ubuntu-latest upgrade path
#   sdist                    — source fallback
#
# To update: bump the version, fetch new hashes from https://pypi.org/pypi/pyyaml/json
# then replace all three hashes below.
pyyaml==6.0.3 \
    --hash=sha256:ba1cc08a7ccde2d2ec775841541641e4548226580ab850948cbfda66a1befcdc \
    --hash=sha256:0f29edc409a6392443abf94b9cf89ce99889a1dd5376d94316ae5145dfedd5d6 \
    --hash=sha256:d76623373421df22fb4cf8817020cbb7ef15c725b9d5e45f17e189bfc384190f
