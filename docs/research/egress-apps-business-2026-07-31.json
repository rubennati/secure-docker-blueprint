{
  "confirmed": [
    {
      "stack": "apps/lycheeorg",
      "project": "Lychee",
      "makes_unrequested_call": true,
      "endpoint": "https://lycheeorg.dev/update.json (vendor-hosted version feed) and https://api.github.com/repos/LycheeOrg/Lychee/security-advisories (vendor's advisory feed). Not reached in the Docker image: https://api.github.com/repos/LycheeOrg/Lychee/{commits,tags} (git-checkout installs only), https://keygen.lycheeorg.dev/api (licence, needs KEYGEN_API_KEY), https://nominatim.openstreetmap.org (location_decoding default '0'), AI_VISION_* (operator-supplied URL, default empty).",
      "data_sent": "NO PERSISTENT INSTALLATION ID. Both are plain HTTP GETs via file_get_contents() with a stream context — no request body, no query string, no version, no instance URL, no email, no licence key. Headers are only 'User-Agent: ' . ini_get('user_agent') (empty in the shipped image unless php.ini sets it) plus, for the advisory feed, 'Accept: application/vnd.github+json'. The full advisory list is downloaded and matched against the local version client-side, so the running version is never disclosed outbound. The receiving host learns the server's source IP and the request timing, nothing more. This is a download, not a beacon.",
      "on_by_default": true,
      "disable_by": "Advisory feed: VULNERABILITY_CHECK_ENABLED=false (env) — a real guard, checked in SecurityAdvisoriesService::getMatchingAdvisories() before the request; or repoint ADVISORIES_API_URL / raise ADVISORIES_CACHE_TTL_DAYS (default 1 day). Update feed: UPDATE_CHECK_ENABLED=false does NOT actually stop it on the /Admin/UpdateStatus route — AdminUpdateStatusController takes CheckUpdate as a constructor-injected dependency, and CheckUpdate::__construct() calls $this->file_version->hydrate() unconditionally, so the fetch has already fired by the time show() evaluates Features::inactive('update-check'). The admin UI setting 'check_for_updates' (DB configs table, default '0' = OFF) only gates the separate VersionResource path, not this one. The response is cached for 'update_check_every_days' (default 3), so worst case is one call per 3 days; short of egress filtering there is no supported switch for it.",
      "evidence": "Endpoints: config/urls.php at tag v7.7.1 — 'json' => 'https://lycheeorg.dev/update.json'; advisories api_url => env('ADVISORIES_API_URL', 'https://api.github.com/repos/LycheeOrg/Lychee/security-advisories'). Defaults ON: config/features.php v7.7.1 line 196 'vulnerability-check' => (bool) env('VULNERABILITY_CHECK_ENABLED', true) and line 208 'update-check' => (bool) env('UPDATE_CHECK_ENABLED', true); the project's own spec docs/specs/4-architecture/features/032-security-advisories/spec.md NFR-032-03 states the flag 'must default to true (opt-out model)'. Unrequested trigger: resources/js/v7/views/LoginPage.vue:66 calls advisoryCheck() after login, and resources/js/composables/modals/useAdvisoryModal.ts fires GET /api/v2/Security/Advisories for any admin, once per browser session; resources/js/v7/views/admin/AdminDashboard.vue onMounted() calls loadUpdateStatus() + loadAdvisories() with no user gesture. Server side: app/Http/Controllers/Admin/AdminUpdateStatusController.php (ctor injects CheckUpdate) -> app/Actions/InstallUpdate/CheckUpdate.php ctor 'hydrate()' -> app/Metadata/Versions/FileVersion.php:55 resolve(UpdateRequest::class)->get_json(). Payload shape: app/Metadata/Json/ExternalRequestFunctions.php fetchFromServer() — GET, timeout 1, header array_merge(['User-Agent: ' . ini_get('user_agent')], $extra_headers); app/Metadata/Json/AdvisoriesRequest.php adds only the Accept header. Not reached in this stack: .dockerignore excludes .git, so GitHubVersion::isGit() returns false and hydrate() returns early before any api.github.com commits/tags call; KeygenApiTokenCheck.php:41-44 and app/Jobs/RotateLicenseKeyJob.php:33-36 both return early when config('verify.keygen_api_key') is '' (KEYGEN_API_KEY commented out in .env.example), and licence validation itself is offline (LycheeOrg/verify src/Verify.php resolve_status() signs url+email locally); config/services.php defaults AI_VISION_FACE_URL/AI_VISION_NSFW_URL/LOCAL_GEO_DECODING_URL to ''; database/migrations/2020_02_14_0600_location_decoding.php sets location_decoding value '0'. A grep of app/ config/ database/ routes/ for telemetry|analytics|matomo|plausible|posthog|mixpanel|sentry|bugsnag|installation_id|instance_id returned only comment-text false positives — there is no analytics or usage-statistics reporting. Local stack files reviewed: /Users/rb3nt/Code/secure-docker-blueprint/apps/lycheeorg/docker-compose.yml and .env.example (APP_TAG=v7.7.1) set none of these variables, so upstream defaults apply as-is.",
      "confidence": "source-read",
      "verdict": {
        "stack": "apps/lycheeorg (Lychee v7.7.1, image lycheeorg/lychee:v7.7.1)",
        "refuted": false,
        "reason": "Verified by cloning LycheeOrg/Lychee at tag v7.7.1 (git describe confirms v7.7.1) and checking every cited file. All five refutation angles fail:\n\nENDPOINTS CORRECT. config/urls.php has 'json' => 'https://lycheeorg.dev/update.json' and advisories api_url => env('ADVISORIES_API_URL', 'https://api.github.com/repos/LycheeOrg/Lychee/security-advisories'), verbatim as claimed.\n\nDEFAULT ON. config/features.php:196 'vulnerability-check' => (bool) env('VULNERABILITY_CHECK_ENABLED', true) and :208 'update-check' => (bool) env('UPDATE_CHECK_ENABLED', true). Neither var appears in upstream .env.example, and neither appears in the local docker-compose.yml or .env.example, so upstream defaults apply unchanged. Spec NFR-032-03 confirms the opt-out model verbatim.\n\nSHIPPED SERVER, NOT A BUILD STEP. Both are runtime Laravel routes: routes/api_v2.php:254 GET /Admin/UpdateStatus -> AdminUpdateStatusController, and :264 GET /Security/Advisories -> SecurityAdvisoriesController. The outbound call chain is real: AdminUpdateStatusController ctor injects CheckUpdate -> CheckUpdate ctor calls $this->file_version->hydrate() -> FileVersion.php:52-56 resolve(UpdateRequest::class)->get_json() -> ExternalRequestFunctions::fetchFromServer() -> file_get_contents(). Only routes/console.php schedules anything (a weekly photos_added_notification) — no scheduled telemetry.\n\nNOT THE OPERATOR'S OWN REQUEST. The operator configures none of these; the URLs are hardcoded upstream defaults. Triggers need no user gesture: resources/js/v7/views/LoginPage.vue:66 calls advisoryCheck() inside goBack(), which is the @logged-in handler for both LoginForm and WebauthnModal; AdminDashboard.vue:278-282 onMounted() calls loadUpdateStatus() + loadAdvisories() when can_edit. Both fire on ordinary admin login/navigation.\n\nPAYLOAD CLAIM HOLDS. fetchFromServer() (ExternalRequestFunctions.php:111-138) builds only method GET, timeout 1, header array_merge(['User-Agent: ' . ini_get('user_agent')], $extra_headers). No body, no query string, no version, no instance URL. AdvisoriesRequest adds only 'Accept: application/vnd.github+json'. SecurityAdvisoriesService::getMatchingAdvisories() downloads the full list and filters against the local version locally, so the running version is never disclosed outbound. Confirmed no persistent installation ID: the telemetry/analytics/installation_id grep over app/ config/ database/ routes/ returns only three unrelated matches (a MetricsPolicy README line, \"plausible\" in ImagickHandler.php:50, \"plausible deniability\" in HasVisitorIdTrait.php:33).\n\n\"NOT REACHED\" EXCLUSIONS HOLD. .dockerignore lists .git, so GitHubVersion::hydrate() returns early at the !$this->isGit() guard before any commits/tags call. KeygenApiTokenCheck.php and RotateLicenseKeyJob.php both return early on config('verify.keygen_api_key') === '' (KEYGEN_API_KEY is commented out at .env.example:191). Nominatim is reached only via Geodecoder with location_decoding, whose migration seeds value '0'.\n\nAlso worth flagging, and it strengthens rather than weakens the claim: the documented opt-out is incomplete for the update feed. AdminUpdateStatusController declares CheckUpdate as a constructor dependency, so Laravel resolves it — and runs hydrate(), which performs the outbound fetch — before show() evaluates Features::inactive('update-check'). Setting UPDATE_CHECK_ENABLED=false suppresses the response payload but does not prevent the request to lycheeorg.dev when that route is hit.",
        "corrected": "The claim stands as written. Two minor citation errors worth fixing, neither load-bearing:\n\n1. File mis-attribution in a side note about calls NOT made: the AI vision defaults are in config/features.php:244 ('face-url' => env('AI_VISION_FACE_URL', '')) and :248 ('nsfw-url' => env('AI_VISION_NSFW_URL', '')), not config/services.php. LOCAL_GEO_DECODING_URL does not exist anywhere in v7.7.1 — a repo-wide grep returns nothing. The conclusion (these endpoints are unset by default and not reached) is still correct.\n\n2. \"matched against the local version client-side\" is imprecise: the matching happens server-side in PHP (app/Services/SecurityAdvisoriesService.php:56-118), not in the browser. The operative point the phrase supports — that the running version is never sent outbound — is correct and verified.\n\nOne addition: setting UPDATE_CHECK_ENABLED=false does not stop the outbound call to lycheeorg.dev on GET /api/v2/Admin/UpdateStatus, because CheckUpdate is constructor-injected and its ctor calls hydrate() before the feature flag is checked in show(). Operators wanting no outbound traffic must block it at the network layer (this stack's app service is on proxy-public, so it has egress) rather than relying on that env var alone. VULNERABILITY_CHECK_ENABLED=false does work as an opt-out, since SecurityAdvind... SecurityAdvisoriesService checks the flag before calling get_json()."
      }
    },
    {
      "stack": "apps/photoprism",
      "project": "PhotoPrism (photoprism/photoprism)",
      "makes_unrequested_call": true,
      "on_by_default": true,
      "endpoint": "https://my.photoprism.app/v1/hello (POST on first run, PUT on renewal; test-only alternative https://hub-int.photoprism.app/v1/hello). Secondary, also on by default: https://places.photoprism.app/v1/location/{s2cell} , /v1/reverse , /v1/search for reverse geocoding of indexed photos. Vendor's own firewall doc lists dl./my./cdn./charts./maps./places.photoprism.app as required outbound hosts.",
      "data_sent": "JSON body of hub.Request: ClientVersion, ClientSerial, ClientOS, ClientArch, ClientCPU (core count), ClientEnv, ClientOpt, PartnerID, ApiToken. PERSISTENT INSTALLATION ID: YES — ClientSerial is a random UID generated once by Config.InitSerial() and written to <storage>/serial plus a copy under the backup path, re-read on every start (readSerial/restoreSerial), so it is stable for the life of the install and turns the daily call into a per-install beacon. The response returns the maps/places API key+secret and the membership tier (licence/entitlement check). The Places calls additionally ship S2 cell IDs / coordinates of the operator's own photos to the vendor during indexing, authenticated with the hub key tied to that serial.",
      "disable_by": "No supported off switch for the hub call. hub.Disable() (baseURL=\"\") is reachable only through hub.ApplyTestConfig(), which is called solely from test helpers (internal/config/test.go, *_test.go) and keys off PHOTOPRISM_TEST_HUB. An options field NoHub (`flag:\"no-hub\"`) exists in internal/config/options.go but no matching CLI flag/env var is registered in internal/config/flags.go, so it can never be set. PHOTOPRISM_DISABLE_PLACES=true (CLI --disable-places, default false) stops the places.photoprism.app geocoding traffic only — it does not stop the my.photoprism.app hello; initHub() has no DisablePlaces check. Practical mitigation is egress filtering at the network level. In this repo apps/photoprism/docker-compose.yml sets neither, so both are active as shipped.",
      "evidence": "Source read (branch develop): https://raw.githubusercontent.com/photoprism/photoprism/develop/internal/service/hub/service.go — `ProdBaseURL = \"https://my.photoprism.app/v1/hello\"`, `var baseURL = ProdBaseURL` (enabled by default; Disable() only sets baseURL=\"\"). https://raw.githubusercontent.com/photoprism/photoprism/develop/internal/service/hub/request.go — Request{ClientVersion, ClientSerial, ClientOS, ClientArch, ClientCPU, ClientEnv, ClientOpt, PartnerID, ApiToken}. https://raw.githubusercontent.com/photoprism/photoprism/develop/internal/service/hub/config.go:206-257 — ReSync() marshals NewRequest(c.Version, c.Serial, c.Env, c.PartnerID, token) and POSTs/PUTs it to GetServiceURL(). https://raw.githubusercontent.com/photoprism/photoprism/develop/internal/config/config.go — Init() calls InitSerial() then Propagate(); Propagate() ends with `c.Hub().Propagate()` -> initHub() (l.980-1020): `update := c.start` (start = command name \"start\") triggers RenewApiKeys(), then a ticker of `23h + rand*2h` calls RenewApiKeys() again — i.e. once per server start and roughly daily thereafter, with no DisablePlaces/NoHub guard. Serial persistence: config.go InitSerial()/readSerial()/serialFiles() write and re-read <storage>/serial (+ backup copy). Places: https://raw.githubusercontent.com/photoprism/photoprism/develop/internal/service/hub/places/config.go — LocationServiceUrls/ReverseServiceUrls/SearchServiceUrls all https://places.photoprism.app/v1/... ; internal/config/config_places.go GeoApi() returns \"places\" unless options.DisablePlaces. Defaults: https://docs.photoprism.app/getting-started/config-options/ — --disable-places default false. Vendor doc: https://docs.photoprism.app/getting-started/troubleshooting/firewall/ lists my.photoprism.app (\"Member Portal\") and places.photoprism.app (\"Reverse Geocoding API\") as required outbound hosts. Maintainer corroboration: https://github.com/photoprism/photoprism/discussions/3425 — connects to the backend once a day to fetch the maps key and check the membership tier. Local stack: /Users/rb3nt/Code/secure-docker-blueprint/apps/photoprism/docker-compose.yml sets PHOTOPRISM_PLACES_LOCALE but no PHOTOPRISM_DISABLE_PLACES. Note: PHOTOPRISM_INIT=\"tensorflow\" in that compose does pull models from dl.photoprism.app, but that is operator-requested and out of scope; maps.photoprism.app tiles are browser-side and also out of scope.",
      "confidence": "source-read",
      "verdict": {
        "stack": "apps/photoprism",
        "refuted": false,
        "reason": "CONFIRMED — every cited line verified against a shallow clone of photoprism/photoprism@develop, not just the raw URLs.\n\nENDPOINT CORRECT: internal/service/hub/service.go has verbatim `ProdBaseURL = \"https://my.photoprism.app/v1/hello\"`, `TestBaseURL = \"https://hub-int.photoprism.app/v1/hello\"`, and `var baseURL = ProdBaseURL`. Places URLs in internal/service/hub/places/config.go are exactly the three https://places.photoprism.app/v1/{location/%s,reverse,search} claimed.\n\nPAYLOAD CORRECT: internal/service/hub/request.go Request struct fields match the claim one-for-one (ClientVersion, ClientSerial, ClientOS, ClientArch, ClientCPU int, ClientEnv, ClientOpt, PartnerID, ApiToken); NewRequest fills OS/Arch/CPU from runtime. hub/config.go ReSync() marshals NewRequest(c.Version, c.Serial, c.Env, c.PartnerID, token) and sends POST when c.Key == \"\" else PUT to GetServiceURL(c.Key) — the first-run/renewal split is accurate.\n\nSHIPPED SERVER, NOT A BUILD STEP: the released image's own command is CMD [\"/opt/photoprism/bin/photoprism\", \"start\"] (docker/photoprism/noble|bullseye|buster|lunar|mantic/Dockerfile). config.go:160-165 NewConfig sets `start = ctx.Command.Name == \"start\"`, so c.start is true for the container's default command. initHub() (config.go:980-1019) does `update := c.start` -> RenewApiKeys(), then starts a goroutine with `d := 23*time.Hour + rand*2h` ticker calling RenewApiKeys() again. Propagate() ends with `c.Hub().Propagate()`, and Init() calls InitSerial() then Propagate(). So: one call per server start, ~daily thereafter, in the long-running server process. Not CI, not an installer, not a client.\n\nON BY DEFAULT / NO OPERATOR OPT-OUT: I actively searched for a refutation and found none. initHub() and RenewApiKeys() contain no DisablePlaces or NoHub guard — RenewApiKeys only checks `c.hub == nil`, and initHub itself assigns c.hub. The only two things that clear baseURL are (a) hub.Disable(), called in exactly one non-test place, internal/commands/config.go:28 InitCoreConfig, which serves the config-report CLI commands and not `start`; and (b) hub.ApplyTestConfig(), which defaults to Disable() but appears only in internal/config/test.go and *_test.go. internal/service/hub/debug.go can override the URL but is behind `//go:build debug`, a tag the Makefile uses only for test targets, so it is absent from release builds. --disable-places is a cli.BoolFlag in flags.go:503 with no Value field, i.e. default false — opt-out, not opt-in.\n\nPERSISTENT INSTALL ID: confirmed exactly. InitSerial() generates rnd.GenerateUID(serialPrefix) once, writes <storage>/serial, and restoreSerial() writes every entry of serialFiles() — which includes c.BackupPath(serialName). readSerial()/readSerialFile() re-read it on each start, and restoreSerial heals a deleted copy from the surviving one. The serial is therefore stable for the life of the install and makes the recurring call a per-install beacon.\n\nPLACES DATA EXPOSURE: confirmed. places/cell.go:56 builds `reqUrl = fmt.Sprintf(serviceUrl, id)`, placing the S2 cell ID derived from the operator's own photo coordinates into the path sent to places.photoprism.app. places/request.go GetRequest sets X-Key: Key and X-Signature: sha1(Key+reqUrl+Secret). hub/config.go Propagate() is literally `places.Key = c.Key; places.Secret = c.Secret`, so those lookups are authenticated with the credentials the hello call returned for that serial — the claimed linkage holds. Reached during indexing via config.go:457 `entity.GeoApi = c.GeoApi()` (entity.go:31 defaults to \"places\") and entity/photo_location.go:368 `loc.Find(GeoApi)` -> Cell.Find -> QueryApi.\n\nNOT THE OPERATOR'S OWN REQUEST: /Users/rb3nt/Code/secure-docker-blueprint/apps/photoprism/docker-compose.yml sets PHOTOPRISM_PLACES_LOCALE and PHOTOPRISM_INIT=\"tensorflow\" but nothing that requests a licence/membership check. The claim already correctly scopes out dl.photoprism.app (operator-requested via PHOTOPRISM_INIT) and browser-side maps.photoprism.app tiles.",
        "corrected": "The claim stands as written. One sharpening for the operator, which makes it slightly worse rather than better: PHOTOPRISM_DISABLE_PLACES=true does NOT stop the my.photoprism.app/v1/hello call. initHub() never consults options.DisablePlaces, and RenewApiKeys() only checks `c.hub == nil`; DisablePlaces gates only GeoApi()/entity.GeoApi, i.e. the places.photoprism.app geocoding lookups. There is no supported environment variable or CLI flag in a release build that mutes the startup + ~daily hello beacon — hub.Disable() is reachable only from the config-report CLI commands and ApplyTestConfig() is test-only. The sole operator-side mitigation is egress filtering (block my.photoprism.app, and places.photoprism.app if geocoding is also unwanted); note the server tolerates failure, logging a debug message pointing at the vendor firewall doc rather than erroring out."
      }
    },
    {
      "stack": "business/documenso",
      "project": "Documenso (documenso/documenso)",
      "makes_unrequested_call": true,
      "on_by_default": true,
      "endpoint": "https://eu.i.posthog.com (PostHog EU Cloud ingestion, via posthog-node). Project key phc_QfqRYkAjBcAv44LI0N2aR0ywUZ9OBXMsmmtAAfjAnUK is baked into the published image as ENV NEXT_PRIVATE_TELEMETRY_HOST / NEXT_PRIVATE_TELEMETRY_KEY. SECONDARY (not default-on): https://license.documenso.com/api/license, POSTed only when the operator sets NEXT_PRIVATE_DOCUMENSO_LICENSE_KEY.",
      "data_sent": "PostHog capture() calls with distinctId = installationId. Two events: 'telemetry_selfhoster_startup' once at server boot, then 'telemetry_selfhoster_heartbeat' every 60 minutes for as long as the container runs. Properties: appVersion, installationId, nodeId. PERSISTENT INSTALLATION ID: YES, and it is the primary identifier — prefixedId('installation') is written to the Postgres site_settings row 'telemetry.installation' and reused forever, so it survives container restarts, image upgrades and re-deploys. A second persistent id, nodeId (prefixedId('node')), is written to <os.tmpdir()>/.documenso-node-id to count instances per cluster. The PostHog client is constructed with disableGeoip: false, so PostHog also resolves the server's source IP to geolocation. No document contents, filenames, user or email data are in the payload. The hourly heartbeat plus stable installation ID makes this a per-install uptime beacon, not a version lookup.",
      "disable_by": "Env var DOCUMENSO_DISABLE_TELEMETRY=true (any truthy value; checked at process start, telemetry client then never initialises). Also auto-disabled when NEXT_PRIVATE_DOCUMENSO_LICENSE_KEY is set. Note: the project's own docker/production/compose.yml does NOT set it, so the documented deployment path runs with telemetry ON.",
      "confidence": "source-read",
      "evidence": "Vendor docs state it outright: https://docs.documenso.com/developers/self-hosting/telemetry — 'This telemetry is enabled by default, but you can easily disable it if you prefer'; collects app version, installation ID, node ID. Source: https://raw.githubusercontent.com/documenso/documenso/main/packages/lib/server-only/telemetry/telemetry-client.ts — TELEMETRY_DISABLED = !!process.env.DOCUMENSO_DISABLE_TELEMETRY || HAS_LICENSE_KEY; new PostHog(TELEMETRY_KEY, { host: TELEMETRY_HOST, disableGeoip: false }); captureEvent('telemetry_selfhoster_startup') then setInterval(...,'telemetry_selfhoster_heartbeat', HEARTBEAT_INTERVAL_MS = 60*60*1000); distinctId = installationId; getOrCreateInstallationId() persists via upsertSiteSetting({ id: SITE_SETTINGS_TELEMETRY_ID }); getOrCreateNodeId() writes path.join(os.tmpdir(), '.documenso-node-id'). ID key = 'telemetry.installation' in packages/lib/server-only/site-settings/schemas/telemetry.ts. Call site: apps/remix/server/router.ts lines 144-147 — `if (env('NODE_ENV') !== 'development') { void TelemetryClient.start(); }` at module load, i.e. every server boot. Credentials ARE shipped, not left blank: docker/Dockerfile lines 54-59 and 95-100 declare ARG/ENV NEXT_PRIVATE_TELEMETRY_KEY and NEXT_PRIVATE_TELEMETRY_HOST with the comment 'Telemetry credentials (baked into image at build time, can be disabled at runtime)', and .github/workflows/publish.yml lines 46-59 pass them from repo secrets into the published build. Verified against the real artifact: I read the image config blob for docker.io/documenso/documenso:latest (manifest digest sha256:845a2a7342c627761fc8b24927e229240e918705abc3289dbdc5b9fb50578bbf, config sha256:123863e3f7e5d074593f0c40e622c4d9c09f0ec5627c41284d4b4bbd1b913995, image created 2026-07-21) and its Env contains NEXT_PRIVATE_TELEMETRY_HOST=https://eu.i.posthog.com and NEXT_PRIVATE_TELEMETRY_KEY=phc_QfqRYkAjBcAv44LI0N2aR0ywUZ9OBXMsmmtAAfjAnUK. Licence check: packages/lib/server-only/license/license-client.ts — LICENSE_SERVER_URL defaults to 'https://license.documenso.com', LicenseClient.start() runs on every boot (router.ts line 151) but pingLicenseServer() returns null immediately if no LICENSE_KEY, so no call by default. AI features are NOT an egress path by default: GOOGLE_VERTEX_API_KEY is empty in .env.example and requests are gated on IS_AI_FEATURES_CONFIGURED plus a per-team aiFeaturesEnabled flag (apps/remix/server/api/ai/detect-recipients.ts), so they only fire after the operator configures a key and a user invokes them. NEXT_PUBLIC_POSTHOG_KEY is blank by default and is browser-side anyway (out of scope).",
      "verdict": {
        "stack": "business/documenso",
        "refuted": false,
        "reason": "Could not refute; every load-bearing element independently confirmed, including against the real published artifact.\n\nIMAGE (decisive, content-addressed): I fetched the Docker Hub manifest for documenso/documenso:latest myself. amd64 manifest digest sha256:845a2a7342c627761fc8b24927e229240e918705abc3289dbdc5b9fb50578bbf and config digest sha256:123863e3f7e5d074593f0c40e622c4d9c09f0ec5627c41284d4b4bbd1b913995 both match the claim; the config blob's own SHA-256 recomputes to its digest, so it is genuinely the published config. created=2026-07-21T05:10:45Z. Its Env contains exactly NEXT_PRIVATE_TELEMETRY_KEY=phc_QfqRYkAjBcAv44LI0N2aR0ywUZ9OBXMsmmtAAfjAnUK and NEXT_PRIVATE_TELEMETRY_HOST=https://eu.i.posthog.com. Cmd=[\"sh\",\"start.sh\"]. Endpoint is correct.\n\nSHIPPED SERVER, not build/CI/installer: apps/remix/server/router.ts calls `void TelemetryClient.start()` at module load, guarded only by `if (env('NODE_ENV') !== 'development')`. NODE_ENV is NOT in the image Env and docker/start.sh does not set it (verified), so the guard passes and telemetry starts on every boot of the runtime process (node build/server/main.js).\n\nDEFAULT-ON, no opt-in gate: telemetry-client.ts has TELEMETRY_DISABLED = !!process.env.DOCUMENSO_DISABLE_TELEMETRY || HAS_LICENSE_KEY; both unset by default. No consent prompt exists — opt-out only. Vendor docs confirm verbatim: \"This telemetry is enabled by default, but you can easily disable it if you prefer.\"\n\nNOT THE OPERATOR'S OWN REQUEST: the PostHog project key is Documenso's, injected from repo secrets by .github/workflows/publish.yml (--build-arg NEXT_PRIVATE_TELEMETRY_KEY/HOST from ${{ secrets.* }}), never configured by the operator.\n\nPAYLOAD/BEHAVIOUR verified verbatim in source: new PostHog(TELEMETRY_KEY, { host: TELEMETRY_HOST, disableGeoip: false }); captureEvent('telemetry_selfhoster_startup') then setInterval(captureEvent('telemetry_selfhoster_heartbeat'), HEARTBEAT_INTERVAL_MS = 60*60*1000); capture({ distinctId: installationId, properties: { appVersion, installationId, nodeId } }). getOrCreateInstallationId() persists via upsertSiteSetting({ id: SITE_SETTINGS_TELEMETRY_ID }), and SITE_SETTINGS_TELEMETRY_ID = 'telemetry.installation' (confirmed). getOrCreateNodeId() writes path.join(os.tmpdir(), '.documenso-node-id'). No document contents, filenames, user or email data. Hourly heartbeat + stable install ID = per-install uptime beacon, as characterised.\n\nSECONDARY licence claim also holds: license-client.ts LICENSE_SERVER_URL defaults to 'https://license.documenso.com' (overridable via INTERNAL_OVERRIDE_LICENSE_SERVER_URL), path 'api/license', LicenseClient.start() runs unconditionally on boot but pingLicenseServer() returns null without a request when no LICENSE_KEY — correctly labelled not-default-on.",
        "corrected": "Claim stands. Three refinements for accuracy, none of which weaken it:\n\n1) SCOPE — official images only. docker/Dockerfile defaults both to empty (ARG NEXT_PRIVATE_TELEMETRY_KEY=\"\" / ARG NEXT_PRIVATE_TELEMETRY_HOST=\"\"), and the client returns early logging \"[Telemetry] Telemetry credentials not configured\" when either is blank. So an image SELF-BUILT from the repo has telemetry OFF; it is on specifically in the OFFICIAL PUBLISHED documenso/documenso images, where publish.yml injects the repo secrets. Operators pulling the published image are affected; operators building their own image are not. (The claim already scoped this to the published image and verified it there — worth stating explicitly so a self-builder is not misinformed.)\n\n2) VENDOR DISCLOSURE IS THINNER THAN THE CLAIM. The docs page says data goes to \"our telemetry servers\" and does not name PostHog, the third-party processor, or the eu.i.posthog.com endpoint. The PostHog EU destination and disableGeoip:false (server source IP resolved to geolocation by PostHog) are visible only from source plus the image Env. The claim is therefore MORE precise than the vendor's own disclosure, not less.\n\n3) nodeId is ephemeral, not persistent. It is written to os.tmpdir()/.documenso-node-id, so in a container it resets on restart and is lost on read-only filesystems (code falls back to in-memory). Only installationId — Postgres site_settings row 'telemetry.installation' — is the durable identifier surviving restarts, upgrades and re-deploys. The claim's framing of installationId as the primary persistent ID is correct; calling nodeId \"persistent\" overstates it slightly.\n\nMinor implementation note: DOCUMENSO_DISABLE_TELEMETRY is evaluated with !! on the raw string, so DOCUMENSO_DISABLE_TELEMETRY=false also disables telemetry (non-empty string is truthy). Documented opt-out is DOCUMENSO_DISABLE_TELEMETRY=true plus a restart; setting NEXT_PRIVATE_DOCUMENSO_LICENSE_KEY also disables it."
      }
    },
    {
      "stack": "apps/homarr",
      "project": "Homarr (homarr-labs/homarr)",
      "makes_unrequested_call": true,
      "on_by_default": true,
      "endpoint": "https://hog.homarr.dev (self-hosted PostHog run by homarr-labs; project key phc_vYBmGWNbRshvfeC7EHfeSmUm2pD2Neg5nGqzJuGvS8Hs) for telemetry. Secondary: api.github.com (Octokit repos.listReleases for homarr-labs/homarr) for the update check.",
      "data_sent": "PERSISTENT INSTALLATION ID: YES. send-server-analytics.ts calls getOrCreateInstanceId(), which mints a cuid on first run and writes it into the DB server setting analytics.instanceId; it is reused forever (survives restarts as long as ./volumes/appdata persists) as the PostHog distinctId. Event name 'server-analytics'. Payload: homarrVersion, databaseType (sqlite/mysql/postgresql), dockerEnabled, kubernetesEnabled, enabled authProviders (credentials/oidc/ldap), process.platform, process.arch, uptimeSeconds, defaultLocale, and row counts for boards, groups, apps, sections, searchEngines, iconRepositories, icons, layouts, itemLayouts, sectionLayouts, cronJobConfigs, customWidgets, trustedCertificates, users, apiKeys, invites, medias, sessions, accounts — plus one property per configured integration kind (integration_<kind>) and per widget kind (widget_<kind>) with counts. No URLs, names, secrets or credentials. Schedule: cron job 'analytics', EVERY_WEEK with runOnStart:true, hidden:true, preventManualExecution:true, preventCustomInterval:true — so it fires once at container start and weekly thereafter. NOTE the docs are stale: homarr.dev/docs/management/settings still describes Umami and says integration data, widget data and user data are separate OPT-IN toggles 'disabled by default'; at v1.72.0 there is a single enableGeneral switch and all of that data ships in the one payload. The update check sends no install ID (only the source IP and a normal GitHub API request), runs at startup via warmUpdateCheckerAsync() and is cached 30 min; the admin UI can re-query it. A third weekly job (iconsUpdater, runOnStart:true) refreshes the configured icon repositories from their remote sources.",
      "on_by_default_note": "",
      "disable_by": "Env var NO_EXTERNAL_CONNECTION=true (default false, documented in apps/docs/docs/advanced/environment-variables) — this is the only switch that also kills the GitHub update check and the icon-repository refresh. Telemetry alone: UI toggle Manage -> Settings -> Analytics -> 'general' switch, i.e. the DB server setting analytics.enableGeneral (default true). The blueprint's apps/homarr/docker-compose.yml sets only TZ and SECRET_ENCRYPTION_KEY, so as shipped both the weekly PostHog beacon and the startup GitHub call are ON.",
      "confidence": "source-read",
      "evidence": "https://raw.githubusercontent.com/homarr-labs/homarr/v1.72.0/packages/analytics/src/client.ts (POSTHOG_HOST = \"https://hog.homarr.dev\", hardcoded project key); https://raw.githubusercontent.com/homarr-labs/homarr/v1.72.0/packages/analytics/src/send-server-analytics.ts (getOrCreateInstanceId writes analytics.instanceId to the DB and uses it as distinctId; full property list; guarded only by analyticsSettings.enableGeneral); https://raw.githubusercontent.com/homarr-labs/homarr/v1.72.0/packages/server-settings/src/index.ts (defaultServerSettings.analytics = { enableGeneral: true, instanceId: null }); https://raw.githubusercontent.com/homarr-labs/homarr/v1.72.0/packages/cron-jobs/src/jobs/analytics.ts (EVERY_WEEK, runOnStart: true, early-returns only if env.NO_EXTERNAL_CONNECTION); https://raw.githubusercontent.com/homarr-labs/homarr/v1.72.0/packages/common/env.ts (NO_EXTERNAL_CONNECTION: createBooleanSchema(false) — defaults to false); https://raw.githubusercontent.com/homarr-labs/homarr/v1.72.0/packages/request-handler/src/update-checker.ts (Octokit listReleases owner homarr-labs) and https://raw.githubusercontent.com/homarr-labs/homarr/v1.72.0/apps/tasks/src/on-start/index.ts (warmUpdateCheckerAsync called on every start); project docs https://homarr.dev/docs/management/settings/ (\"By default, Homarr is collecting anonymous analytics\") and https://homarr.dev/docs/advanced/environment-variables (NO_EXTERNAL_CONNECTION default false); local /Users/rb3nt/Code/secure-docker-blueprint/apps/homarr/docker-compose.yml sets no NO_EXTERNAL_CONNECTION.",
      "verdict": {
        "stack": "apps/homarr (Homarr, homarr-labs/homarr, pinned APP_TAG=v1.72.0)",
        "refuted": false,
        "reason": "Attempted refutation failed on every check; all cited evidence read directly at tag v1.72.0.\n\nENDPOINT CORRECT. packages/analytics/src/client.ts verbatim: POSTHOG_API_KEY = \"phc_vYBmGWNbRshvfeC7EHfeSmUm2pD2Neg5nGqzJuGvS8Hs\", POSTHOG_HOST = \"https://hog.homarr.dev\". Both hardcoded — not operator-configured, so this is not the operator's own request in disguise.\n\nON BY DEFAULT. packages/server-settings/src/index.ts: analytics: { enableGeneral: true, instanceId: null }. send-server-analytics.ts is guarded ONLY by `if (!analyticsSettings.enableGeneral)`. Second guard is `if (env.NO_EXTERNAL_CONNECTION) return;` in the cron job, and packages/common/env.ts defines NO_EXTERNAL_CONNECTION: createBooleanSchema(false). Local /Users/rb3nt/Code/secure-docker-blueprint/apps/homarr/docker-compose.yml sets only TZ and SECRET_ENCRYPTION_KEY — neither guard is engaged. No feature must be enabled first.\n\nRUNS IN THE SHIPPED SERVER — this was the strongest refutation candidate and it failed. The Dockerfile does NOT copy apps/tasks into the runner stage, and scripts/run.sh starts only nginx, redis and `node apps/nextjs/server.js` — no separate tasks process, which initially suggested dead code. But apps/nextjs/src/instrumentation.ts is: `export async function register() { if (process.env.NEXT_RUNTIME === \"nodejs\") { await import(\"@homarr/tasks\"); ... } }`. The package rides in via Next.js standalone output tracing. apps/tasks/src/main.ts then runs `await onStartAsync(); await jobGroup.initializeAsync(); await jobGroup.startAllAsync();`. packages/cron-jobs/src/index.ts registers analyticsJob in the group. Chain is complete inside the shipped container: not a build step, CI script, installer, or client.\n\nSCHEDULE/ID CONFIRMED. cron-jobs/src/jobs/analytics.ts verbatim: createCronJob(\"analytics\", EVERY_WEEK, { runOnStart: true, preventManualExecution: true, preventCustomInterval: true, hidden: true }) — fires at container start and weekly. getOrCreateInstanceId mints createId() and persists it via updateServerSettingByKeyAsync(db, \"analytics\", {...}), reused forever as PostHog distinctId. Event name \"server-analytics\". Payload list matches, and contains no URLs, hostnames, app names or credentials.\n\nSECONDARY CONFIRMED. update-checker.ts uses octokit.rest.repos.listReleases for homarr-labs/homarr, 30-min cache, sends no instance ID; warmUpdateCheckerAsync() is called unconditionally in apps/tasks/src/on-start/index.ts (the NO_EXTERNAL_CONNECTION guard sits inside the handler, so with defaults the GitHub call does occur).\n\nCode side of the \"docs are stale\" point confirmed: there is exactly one enableGeneral switch, no separate integration/widget/user opt-ins. I did not fetch the homarr.dev docs page itself, and I did not independently verify the iconsUpdater job's EVERY_WEEK/runOnStart flags (only that it is registered) — both are peripheral to the claim.",
        "corrected": "Claim stands. Two cosmetic corrections only, neither affecting the finding: (1) the payload property NAMES are `osPlatform` and `osArch` (their values come from process.platform / process.arch) — the claim named the sources rather than the keys; (2) the claim's per-table list omits the two aggregate properties `countIntegrations` and `countWidgets` that are also sent alongside the per-kind `integration_<kind>` / `widget_<kind>` breakdowns. Also note the trusted-certificate figure is a count of rows in trustedCertificateHostnames — a number only, no hostnames transmitted, consistent with the claim's \"no URLs or names\"."
      }
    },
    {
      "stack": "apps/dashy",
      "project": "Dashy (Lissy93/dashy)",
      "makes_unrequested_call": true,
      "endpoint": "https://raw.githubusercontent.com/Lissy93/dashy/master/package.json (plain GET to raw.githubusercontent.com, hardcoded)",
      "data_sent": "NO PERSISTENT INSTALLATION ID, and no payload of local data at all. It is a one-way download, not a beacon: the request is an unauthenticated GET with no query params and no custom headers (services/utils/request.js only adds `Accept-Encoding: gzip, deflate, br`; no User-Agent is set). The local version is NOT transmitted — `currentVersion` is read from the bundled package.json and compared locally in `compareVersions()`. What GitHub/Fastly therefore observes is only the connection metadata: the server's source IP, TLS SNI for raw.githubusercontent.com, and the timestamp — once per container start (the module runs at require-time, there is no interval/polling). Result is only printed to the container log.\n\nSeparate, browser-side (out of scope, noted for completeness): src/components/Configuration/AppVersion.vue fetches the same URL on component mount; that is a call from the viewer's browser, and it IS gated by `appConfig.disableUpdateChecks`.\n\nNo telemetry/analytics of any kind. Sentry error reporting exists but `appConfig.enableErrorReporting` defaults to **false** (ConfigSchema.json:860-865, \"Reporting is off by default\") and is browser-side. Cloud backup (Cloudflare Worker KV) is off unless a user activates it in the UI and is client-side E2E encrypted. No licence/registration check, no AI feature, no vendor relay.",
      "on_by_default": true,
      "disable_by": "NOTHING, for the server-side call, in 4.5.0. This is the important correction to the vendor docs. `appConfig.disableUpdateChecks: true` (default false, ConfigSchema.json:848-853) only stops the BROWSER check — it is read in AppVersion.vue:60. The server-side check cannot see it: services/app.js line 22 is `require('./utils/update-checker');`, which fires on module load, and the config is not even loaded until line 24 (`require('./utils/config-validator')`). The only guard in update-checker.js is `if (process.env.NODE_ENV !== 'test')`, and the upstream Dockerfile hardcodes `ENV NODE_ENV=production` (this stack's docker-compose.yml also sets `NODE_ENV: production` explicitly). Setting NODE_ENV=test would suppress it but changes app behaviour and is not a supported knob. Practical mitigation for this blueprint is egress filtering, not configuration.",
      "confidence": "source-read",
      "evidence": "Source read at the exact pinned tag 4.5.0 (stack pins APP_TAG=4.5.0 in /Users/rb3nt/Code/secure-docker-blueprint/apps/dashy/.env.example). Server-side: https://raw.githubusercontent.com/Lissy93/dashy/4.5.0/services/utils/update-checker.js — `const packageUrl = 'https://raw.githubusercontent.com/Lissy93/dashy/master/package.json';` and the trailing `// Skip version check when running tests` / `if (process.env.NODE_ENV !== 'test') { runCheck()... }`. Call site: https://raw.githubusercontent.com/Lissy93/dashy/4.5.0/services/app.js line 22 `require('./utils/update-checker'); // Checks if there are any updates available, prints message` (config loaded only at line 24). Request headers: https://raw.githubusercontent.com/Lissy93/dashy/master/services/utils/request.js. Browser-side gate: https://raw.githubusercontent.com/Lissy93/dashy/4.5.0/src/components/Configuration/AppVersion.vue lines 59-70. Defaults: https://raw.githubusercontent.com/Lissy93/dashy/4.5.0/src/utils/config/ConfigSchema.json lines 848-870 (`disableUpdateChecks` default false; `enableErrorReporting` default false). Vendor doc: https://github.com/Lissy93/dashy/blob/master/docs/privacy.md#update-checks — \"the latest version is fetched from GitHub, which is an external request. This can be disabled by setting `appConfig.disableUpdateChecks: true`\", plus \"There are no access logs, no analytics and no telemetry.\" Note the doc is incomplete: it describes only the client-side check, and its stated disable switch does not reach the server-side one. Image confirmed to run `node server.js` with NODE_ENV=production: https://raw.githubusercontent.com/Lissy93/dashy/master/Dockerfile. Local stack files reviewed: /Users/rb3nt/Code/secure-docker-blueprint/apps/dashy/docker-compose.yml, .env.example, config/conf.example.yml (the example config sets no update/telemetry keys).",
      "verdict": {
        "stack": "apps/dashy",
        "refuted": false,
        "reason": "Could not refute — every load-bearing element verified verbatim at the pinned tag 4.5.0 (the stack pins APP_TAG=4.5.0 in /Users/rb3nt/Code/secure-docker-blueprint/apps/dashy/.env.example).\n\nEVIDENCE READ CORRECTLY, NOT MISREAD. services/utils/update-checker.js@4.5.0 is 56 lines, read in full: line 5 `const packageUrl = 'https://raw.githubusercontent.com/Lissy93/dashy/master/package.json';`, line 40 `const response = await request.get(packageUrl);`, lines 53-56 `// Skip version check when running tests` / `if (process.env.NODE_ENV !== 'test') { runCheck().catch(...); }`. Endpoint string is exact.\n\nRUNS IN THE SHIPPED SERVER, not a build step or client. Chain confirmed end to end: Dockerfile@4.5.0 sets `ENV NODE_ENV=production IS_DOCKER=true`, does `COPY --from=build /app/services ./services` and `/app/server.js`, and ends `CMD [\"node\", \"server.js\"]`. server.js:12 `const app = require('./services/app');` (unconditional). services/app.js:22 `require('./utils/update-checker');` — at require-time, before `config-validator` loads at line 24. The local docker-compose.yml also sets `NODE_ENV: production` explicitly, so the only guard (`!== 'test'`) passes.\n\nON BY DEFAULT, no feature needed. grep of update-checker.js for config/env/disable/interval returns only the NODE_ENV test-guard. The module never reads appConfig and there is no env var for it. `disableUpdateChecks` (ConfigSchema.json, default false) is consumed only in src/components/Configuration/AppVersion.vue `mounted()` — browser-side — confirming the claim's key point that the vendor's documented off-switch does not reach the server-side check.\n\nNOT THE OPERATOR'S OWN REQUEST. The URL is a hardcoded module constant, not derived from conf.yml. config/conf.example.yml sets no update/telemetry keys and nothing that resolves to raw.githubusercontent.com.\n\nPAYLOAD CLAIM HOLDS. request.get(packageUrl) is called with no config object, so services/utils/request.js builds `headers: {}` and adds only `Accept-Encoding: gzip, deflate, br` (line 138-140); Authorization is added only for URL-embedded credentials (none here); Node sets no default User-Agent. No query string, no body on GET. `currentVersion` comes from the bundled package.json (line 3) and is only used locally in compareVersions()/makeMsg() — it is never put in the request. No install ID exists.\n\nONCE PER CONTAINER START. runCheck() is invoked once at module load; no setInterval/setTimeout. I specifically checked the 90s compose healthcheck: services/healthcheck.js is a standalone process that requires only fs and http/https and hits localhost /healthz — it does not pull in services/app.js, so it does not re-fire the update check.\n\nTWO MINOR PRECISIONS (do not change the verdict): (1) GitHub/Fastly also see the HTTP request path /Lissy93/dashy/master/package.json, which identifies the software as Dashy — slightly more than the \"IP, SNI, timestamp\" the claim lists. (2) `restart: unless-stopped` in the local compose means every restart or crash-loop iteration repeats the one-shot call.\n\nNOT VERIFIED: the published lissy93/dashy:4.5.0 image was not pulled (not cached locally; a pull needs the operator's go-ahead). The finding rests on the Dockerfile and sources at the 4.5.0 tag, which is the standard basis for this call."
      }
    },
    {
      "stack": "apps/bookstack",
      "project": "BookStack",
      "makes_unrequested_call": true,
      "endpoint": "https://www.gravatar.com/avatar/${hash}?s=${size}&d=identicon (Automattic/Gravatar). No version-check, telemetry, licence or AI endpoint found.",
      "data_sent": "Server-side (PHP HTTP client, not the browser) GET carrying an MD5 hash of the user's e-mail address plus a size parameter, sent to Automattic whenever a user account is created or its avatar is refreshed; the fetched image is then stored locally. NO PERSISTENT INSTALLATION ID is present — this is not a per-install beacon, it is a per-user e-mail-hash lookup, so the leak is user PII (hashed e-mail, plus the instance's egress IP and timing) rather than instance identity. Separately: DRAWIO=true is also on by default but loads embed.diagrams.net in the visitor's browser, so it is out of scope here. No version/update check, no telemetry, no licence check and no default AI integration were found in BookStack's own env reference or config.",
      "disable_by": "Set DISABLE_EXTERNAL_SERVICES=true in BookStack's environment (comment in upstream .env.example.complete: \"Disable default third-party services such as Gravatar and Draw.IO\"). Note AVATAR_URL overrides it — if AVATAR_URL is set, avatars are fetched regardless. This blueprint does NOT set either: /Users/rb3nt/Code/secure-docker-blueprint/apps/bookstack/docker-compose.yml passes a fixed env list that contains neither variable, and neither appears in apps/bookstack/.env.example, so the Gravatar egress is live as shipped.",
      "on_by_default": true,
      "confidence": "source-read",
      "evidence": "https://raw.githubusercontent.com/BookStackApp/BookStack/development/.env.example.complete — \"# Disable default third-party services such as Gravatar and Draw.IO\" / \"DISABLE_EXTERNAL_SERVICES=false\"; and https://raw.githubusercontent.com/BookStackApp/BookStack/development/app/Uploads/UserAvatars.php — default URL \"https://www.gravatar.com/avatar/${hash}?s=${size}&d=identicon\", fetched server-side via $client->sendRequest(new Request('GET', $url)), gated by config('services.disable_services'). Blueprint side: /Users/rb3nt/Code/secure-docker-blueprint/apps/bookstack/docker-compose.yml (app service `environment:` block) sets neither variable. Caveat: the absence of a version/update check rests on it not appearing in BookStack's env reference, config/services.php or the security docs page — that is weak evidence of absence, not proof.",
      "verdict": {
        "stack": "PHP / Laravel (BookStack), deployed as lscr.io/linuxserver/bookstack + MariaDB via Docker Compose at /Users/rb3nt/Code/secure-docker-blueprint/apps/bookstack/",
        "refuted": false,
        "reason": "Could not refute — every check passes against source.\n\n1) EVIDENCE ACCURATE, NOT MISREAD. app/Uploads/UserAvatars.php:190 (getAvatarUrl) contains verbatim: $url = 'https://www.gravatar.com/avatar/${hash}?s=${size}&d=identicon'; reached when `empty($url) && !config('services.disable_services')`. app/Config/services.php:14 'disable_services' => env('DISABLE_EXTERNAL_SERVICES', false) and :20 'avatar_url' => env('AVATAR_URL', '') — both defaults land on the Gravatar fallback.\n\n2) SHIPPED SERVER, NOT BUILD/CI/INSTALLER. saveAvatarImage() computes '${hash}' => md5(strtolower(trim($user->email))), '${size}' => 500, then getAvatarImageData() executes $client->sendRequest(new Request('GET', $url)) through Guzzle (HttpRequestService, 5s timeout, follows up to 3 redirects). Server-side PHP in the running app. Image is then stored locally via createAvatarImageFromData -> imageService->saveNew. Call chain: UserRepo::createWithoutActivity (app/Users/UserRepo.php:99) -> downloadAndAssignUserAvatar (:274) -> UserAvatars::fetchAndAssignToUser.\n\n3) ON BY DEFAULT — CONFIRMED FOR THIS EXACT IMAGE. The blueprint uses lscr.io/linuxserver/bookstack. LSIO's init-bookstack-config/run copies /app/www/.env.example (the SHORT example, not .env.example.complete) to /config/www/.env; that short file sets neither DISABLE_EXTERNAL_SERVICES nor AVATAR_URL. LSIO's Dockerfile sets neither. The blueprint docker-compose.yml app service environment: block sets neither, and grep over the whole apps/bookstack/ dir for gravatar|avatar_url|DISABLE_EXTERNAL|drawio returns nothing. So BookStack's own defaults apply end to end.\n\n4) NOT THE OPERATOR'S OWN REQUEST IN DISGUISE. AVATAR_URL defaults to empty; the Automattic URL is a hardcoded fallback in PHP, never entered by the operator. (Contrast: OidcService.php:231 calls assignToUserFromUrl with the IdP's `picture` claim — that one IS operator-configured and is a different endpoint.)\n\n5) ENDPOINT CORRECT. https://www.gravatar.com/avatar/<md5>?s=500&d=identicon. Note the default template uses only ${hash} and ${size}; the ${email} placeholder exists in the substitution table but is NOT in the default URL, so the plaintext address is not sent — only its MD5.\n\n6) NEGATIVE FINDINGS HOLD. Only outbound hosts in app/*.php that are live calls: www.gravatar.com and embed.diagrams.net (browser-side, correctly scoped out). www.bookstackapp.com appears once, as a documentation comment in app/Search/SearchApiController.php:32 — not a version check. No telemetry, no licence check, no AI endpoint. No persistent installation ID anywhere in the URL, so the claim's framing (user PII, not instance identity) is right.\n\nCORRECTIONS ON TRIGGER TIMING (refinements, not refutations):\n- The seeded initial admin (admin@admin.com) is inserted by a raw DB write in database/migrations/2014_10_12_000000_create_users_table.php and bypasses UserRepo entirely — so a fresh install that never adds a second user makes NO Gravatar call.\n- \"or its avatar is refreshed\" overstates it: refresh is a manual operator-run artisan command (app/Console/Commands/RefreshAvatarCommand.php, `bookstack:refresh-avatar`, dry-run unless --force). app/Console/Kernel.php schedule() is an empty no-op, so there is no automatic or periodic re-fetch.",
        "corrected": "apps/bookstack (BookStack, lscr.io/linuxserver/bookstack) makes an outbound call the operator did not ask for, to https://www.gravatar.com/avatar/${hash}?s=${size}&d=identicon (Automattic/Gravatar).\n\nWhat is sent: a server-side GET (PHP/Guzzle, not the browser) carrying an MD5 hash of the user's lower-cased e-mail address plus s=500&d=identicon. The plaintext address is never sent — the default URL template uses only ${hash} and ${size}. The fetched image is stored locally afterwards.\n\nWhen it fires: on creation of a real user account — admin UI (UserController), API (UserApiController), self-registration (RegistrationService), or first LDAP/SAML login — all routed through UserRepo::createWithoutActivity. It does NOT fire for the seeded default admin (admin@admin.com), which is written by a raw migration that bypasses UserRepo, so a fresh install with only that admin stays silent. There is no automatic or periodic refresh: the Laravel scheduler is empty, and re-fetching requires the operator to run `php artisan bookstack:refresh-avatar` by hand.\n\nDefault state: on. avatar_url defaults to '' and disable_services defaults to false, so the hardcoded Gravatar fallback applies. The LinuxServer image copies the short .env.example on first run, and that file sets neither variable; neither does the LSIO Dockerfile nor the blueprint compose file. Setting DISABLE_EXTERNAL_SERVICES=true (or pointing AVATAR_URL at a self-hosted/Libravatar instance) stops it.\n\nNo persistent installation ID is present — this is a per-user e-mail-hash lookup, not a per-install beacon, so the leak is user PII (hashed e-mail, plus the instance's egress IP and timing) rather than instance identity.\n\nSeparately: DRAWIO=true is also on by default but loads embed.diagrams.net in the visitor's browser, so it is out of scope here.\n\nNo version/update check, no telemetry, no licence check and no AI integration were found. This was checked against the actual source tree, not just the env reference: the only live outbound hosts in app/ are gravatar.com and embed.diagrams.net, and the single bookstackapp.com occurrence is a documentation comment in SearchApiController."
      }
    },
    {
      "stack": "apps/wordpress",
      "project": "WordPress",
      "makes_unrequested_call": true,
      "on_by_default": true,
      "endpoint": "https://api.wordpress.org/core/version-check/1.7/ (plus /plugins/update-check/1.1/ and /themes/update-check/1.1/); requests start as http:// and are upgraded to https:// when the host supports SSL",
      "data_sent": "Core check (GET query args): version, php, locale, mysql, local_package, blogs (site count), users (user count), multisite_enabled, initial_db_version, myisam_tables, extensions (PHP extension list), platform_flags (OS + bitness), image_support (GD/Imagick caps); POST body carries installed translations. PERSISTENT INSTALLATION ID: no random UUID is generated, BUT the site's own URL is transmitted on every core check in the request headers wp_install = home_url('/') (network_site_url() on multisite) and wp_blog = home_url('/'), and again in the User-Agent 'WordPress/<ver>; <home_url>'. A stable site URL plus source IP is a de-facto per-install identifier, so the version lookup is correlatable per install even though no opaque UUID exists. Plugin check POSTs the full list of installed plugins with versions/active state; theme check POSTs the installed themes incl. the active one (these two send only the User-Agent, not the wp_install/wp_blog headers).",
      "disable_by": "No supported env var or UI setting — the official wordpress image and this repo's business/dolibarr-style .env expose nothing for it (apps/wordpress/.env.example has no such key). WP_AUTO_UPDATE_CORE=false and AUTOMATIC_UPDATER_DISABLED stop updates being INSTALLED, not the check itself; DISABLE_WP_CRON only moves cron triggering. Actually stopping it requires code: a mu-plugin that unschedules the wp_version_check / wp_update_plugins / wp_update_themes cron hooks or short-circuits the pre_site_transient_update_core/_update_plugins/_update_themes filters. The core_version_check_query_args filter only trims the payload.",
      "evidence": "https://raw.githubusercontent.com/WordPress/WordPress/master/wp-includes/update.php — wp_version_check(): $url = 'http://api.wordpress.org/core/version-check/1.7/?' . http_build_query($query,'','&'); 'headers' => array('wp_install' => $wp_install, 'wp_blog' => home_url('/')); $wp_install = home_url('/') (else network_site_url() when is_multisite()), ~lines 96-101. wp_update_plugins() POSTs to http://api.wordpress.org/plugins/update-check/1.1/ (~lines 403-411), wp_update_themes() to http://api.wordpress.org/themes/update-check/1.1/ (~lines 591-597). All three are wp_schedule_event(..., 'twicedaily', ...) — hooks wp_version_check, wp_update_plugins, wp_update_themes (~lines 732-741). Stack uses the stock image: apps/wordpress/docker-compose.yml:69 image: wordpress:${APP_TAG}, so upstream default behaviour applies unmodified.",
      "confidence": "source-read",
      "verdict": {
        "stack": "apps/wordpress (WordPress)",
        "refuted": false,
        "reason": "Could not refute — confirmed on all five checks.\n\nEVIDENCE SUPPORTS IT (not misread). Upstream wp-includes/update.php matches the quoted code verbatim: wp_version_check() builds $url = 'http://api.wordpress.org/core/version-check/1.7/?' . http_build_query($query,'','&') and passes 'headers' => array('wp_install' => $wp_install, 'wp_blog' => home_url('/')), with $wp_install = network_site_url() when is_multisite() else home_url('/'), and 'user-agent' => 'WordPress/' . wp_get_wp_version() . '; ' . home_url('/'). The $query arg list is exactly as claimed (version, php, locale, mysql, local_package, blogs, users, multisite_enabled, initial_db_version, myisam_tables, extensions, platform_flags{os,bits}, image_support), and the POST body is $post_body = array('translations' => wp_json_encode($translations)). wp_update_plugins() POSTs to http://api.wordpress.org/plugins/update-check/1.1/ with body plugins/translations/locale/all; wp_update_themes() POSTs to http://api.wordpress.org/themes/update-check/1.1/ with body themes/translations/locale. The claim's subtle distinction is correct: neither the plugin nor theme call carries wp_install/wp_blog headers — only the User-Agent (which still embeds home_url). All three functions contain $ssl = wp_http_supports(array('ssl')); if ($ssl) { $url = set_url_scheme($url, 'https'); }, so the http:// -> https:// upgrade description is right.\n\nSHIPPED SERVER, NOT A BUILD STEP. wp-includes/update.php is core runtime loaded on every request. The three calls are cron-driven via wp_schedule_event(..., 'twicedaily', ...) on hooks wp_version_check, wp_update_plugins, wp_update_themes, plus add_action('admin_init', '_maybe_update_core'). Not CI, not an installer, not a desktop/mobile client.\n\nON BY DEFAULT — NO FEATURE GATE. Stack uses the stock image (apps/wordpress/docker-compose.yml:69 `image: wordpress:${APP_TAG}`, APP_TAG=7.0.2-php8.3-apache). The docker-library entrypoint sets no update-related constants, and wp-config-docker.php defines only DB creds, the eight salts, WP_DEBUG and ABSPATH — no AUTOMATIC_UPDATER_DISABLED, WP_HTTP_BLOCK_EXTERNAL, DISALLOW_FILE_MODS or DISABLE_WP_CRON. The repo's own /Users/rb3nt/Code/secure-docker-blueprint/apps/wordpress/config/mu-plugins/security-hardening.php only blocks REST user enumeration, strips version strings, genericises login errors and disables XML-RPC — nothing touching update checks. DISALLOW_FILE_MODS and WP_AUTO_UPDATE_CORE appear only as optional suggestions in apps/wordpress/README.md (lines ~100-109), are not applied by default, and would not suppress the version lookup anyway (they gate file modification, not the check). WP-Cron is left enabled, so front-end visits fire the events without operator action.\n\nNOT THE OPERATOR'S OWN REQUEST. Nothing in apps/wordpress/.env.example, docker-compose.yml, or config/ references api.wordpress.org; the operator configures no such destination.\n\nENDPOINTS CORRECT. All three verified against upstream.\n\nGuards exist but do not exempt a default install: each function early-returns on wp_installing(), and the registration block at file end is skipped when ( ! is_main_site() && ! is_network_admin() ) || wp_doing_ajax() — neither applies to a normal single-site deployment.\n\nIDENTIFIER FRAMING IS ACCURATE AND APPROPRIATELY HEDGED. No opaque UUID is generated; the claim says so. The correlatable identifier is the site's own URL, transmitted in the wp_install and wp_blog headers on the core check and in the User-Agent on all three calls — which, with source IP, is a de-facto stable per-install identifier. That characterisation is fair.\n\nOnly defect: the cited line numbers (~96-101, ~403-411, ~591-597, ~732-741) do not match current master — see corrected field. Line numbers drift across WordPress releases; the quoted code is correct.",
        "corrected": "The claim stands as written. One non-substantive correction: the cited line numbers do not match current master. In the current WordPress/WordPress master copy of wp-includes/update.php, the wp_version_check() $url assignment sits near line ~135 and its headers array near ~161 (not ~96-101), and the add_action/wp_schedule_event registration block sits near ~940-962 (not ~732-741). Line numbers shift between WordPress releases; the quoted code, endpoints, header names, query args and cron recurrence are all correct as stated. Two optional additions for completeness: the plugin and theme POSTs also carry translations and locale fields (plugins additionally send all => true), and the core check's POST body is exactly $post_body = array('translations' => wp_json_encode($translations))."
      }
    },
    {
      "stack": "business/dolibarr",
      "project": "Dolibarr ERP/CRM",
      "makes_unrequested_call": true,
      "on_by_default": true,
      "endpoint": "https://ping.dolibarr.org/ — default value of the MAIN_URL_FOR_PING constant: $url_for_ping = getDolGlobalString('MAIN_URL_FOR_PING', \"https://ping.dolibarr.org/\"). main.inc.php also widens the Content-Security-Policy to allow it (default-src 'self' ping.dolibarr.org).",
      "data_sent": "POST fields: action=dolibarrping, datesys, country_code (company country from the ERP's own company record), hash_algo, hash_unique_id, version, version_full, versionblockedlog, versionblockedlog_full, entity, dbtype, php_version, os_version, db_version, distrib, token=notrequired. PERSISTENT INSTALLATION ID: YES — hash_unique_id = dol_hash('dolibarr' . $conf->file->instance_unique_id [. entity], 'sha256'), and instance_unique_id is $dolibarr_main_instance_unique_id written into conf.php at install time, so the id survives restarts and container recreation as long as the config volume persists. This makes the call a per-install beacon, not an anonymous version lookup. MECHANISM (important for egress filtering): the server does not open the socket itself — it prints a jQuery $.ajax POST into the home page (index.php) that the logged-in admin's BROWSER executes; the browser then reports success/failure back to the local instance at /core/ajax/pingresult.php, which stores MAIN_FIRST_PING_OK_ID = '<hash> - <version>'. A container-level egress rule therefore will NOT block it. SECOND, non-default beacon: if the BlockedLog module is enabled on an LNE-qualified (French fiscal certification) version, action=dolibarrregistration sends the same endpoint the company name, email, SIREN/SIRET (idprof1/idprof2), full postal address and the IT provider's name, email, phone and address — real business PII, not telemetry. Not on by default (requires BlockedLog + qualified version).",
      "disable_by": "Install wizard (install/step2.php) shows a 'MakeAnonymousPing' checkbox that is CHECKED by default — opt-out, not opt-in — and unchecking only sets a browser cookie DOLINSTALLNOPING_<hash>. This repo's compose does an automated install (DOLI_ADMIN_LOGIN/DOLI_ADMIN_PASSWORD in business/dolibarr/docker-compose.yml:103-104) and passes no ping-related variable, so nobody clicks that box and the default stands. After install: set the constant MAIN_FIRST_PING_OK_ID = 'disabled' (Home > Setup > Other setup, or directly in the llx_const table) — the code explicitly skips the ping on that value — or repoint MAIN_URL_FOR_PING. No environment variable exists for it.",
      "evidence": "https://raw.githubusercontent.com/Dolibarr/dolibarr/develop/htdocs/core/lib/functions2.lib.php:3135-3212 printCodeForPing() — endpoint, jQuery $.ajax POST and the full field list; https://raw.githubusercontent.com/Dolibarr/dolibarr/develop/htdocs/main.inc.php:4027-4065 (ping fires on DOL_URL_ROOT/index.php unless already done for this uniqueid+version; retried next month after failure; &forceping=1 forces) and :1648 (CSP amended for ping.dolibarr.org), :4080-4120 (BlockedLog registration payload with company/provider PII); https://raw.githubusercontent.com/Dolibarr/dolibarr/develop/htdocs/blockedlog/lib/blockedlog.lib.php:186-191 getHashUniqueIdOfRegistration() = dol_hash('dolibarr'.$conf->file->instance_unique_id...); https://raw.githubusercontent.com/Dolibarr/dolibarr/develop/htdocs/install/step2.php checkbox 'dolibarrpingno' rendered with checked=\"true\" unless MAIN_FIRST_PING_OK_ID == 'disabled'; https://raw.githubusercontent.com/Dolibarr/dolibarr/develop/htdocs/core/ajax/pingresult.php stores MAIN_FIRST_PING_OK_ID. Stack uses the stock image: business/dolibarr/docker-compose.yml:69 image: dolibarr/dolibarr:${APP_TAG}.",
      "confidence": "source-read",
      "verdict": {
        "stack": "business/dolibarr",
        "refuted": false,
        "reason": "Core claim verified against the pinned version (APP_TAG=23.0.3), not just the cited `develop` branch. functions2.lib.php:3140 in tag 23.0.3 has verbatim `$url_for_ping = getDolGlobalString('MAIN_URL_FOR_PING', \"https://ping.dolibarr.org/\")`. main.inc.php:3748 fires it on DOL_URL_ROOT/index.php, gated on uniqueid+version, retried next month after failure, forced by &forceping=1. The described mechanism is accurate and material: the PHP server does not open the socket; it prints a jQuery $.ajax POST that the logged-in admin's browser executes, then reports back to /core/ajax/pingresult.php, which at line 87 writes MAIN_FIRST_PING_OK_ID. Container-level egress filtering therefore does not block it. ON BY DEFAULT is confirmed twice: install/step2.php:616 renders the checkbox with checked=\"true\" unless MAIN_FIRST_PING_OK_ID=='disabled' (opt-out, and unchecking only sets a browser cookie DOLINSTALLNOPING_*), AND the official image sets DOLI_INSTALL_AUTO=1, so the wizard never runs and that cookie is never set — the opt-out is unreachable in this stack. Not a build/CI/installer-only path and not an operator-configured request: nothing in business/dolibarr/docker-compose.yml or .env.example asks for it. THREE INACCURACIES, all from citing `develop` for a 23.0.3 deployment. (1) Fields `datesys`, `versionblockedlog`, `versionblockedlog_full` do not exist in 23.0.3 — develop-only. (2) The CSP widening at main.inc.php:1648 is develop-only and is a no-op by default even there: MAIN_SECURITY_FORCECSP is empty, preg_replace on an empty string yields empty, and the header is only emitted `if (!empty($contentsecuritypolicy))` — Dolibarr sends no CSP header by default, so nothing is widened. (3) Most importantly, the \"PERSISTENT PER-INSTALL ID\" framing is wrong for this stack and cuts against the claim: dolibarr-docker images/23.0.3-php8.2/docker-run.sh:85 hardcodes `$dolibarr_main_instance_unique_id='myinstanceuniquekey'` when DOLI_INSTANCE_UNIQUE_ID is unset, and the stack never sets it; MAIN_SECURITY_SALT is empty by default so dol_hash applies no salt. The transmitted hash_unique_id is therefore the fixed constant sha256('dolibarr'+'myinstanceuniquekey') = 8fd1d563da6a3ce1f4b3dda06b90fa285b4bf81b2e55d87de88c1807fc62e945, identical across every Dolibarr Docker install that leaves the variable unset — a shared constant, not a per-install fingerprint. Also, conf.php lives at /var/www/html/conf/conf.php, which is neither a declared VOLUME nor mounted by this compose, so it is regenerated on container recreation (to the same constant). Minor: the 23.0.3 hash has no `. entity` concatenation (entity is a separate field), and the BlockedLog payload sends company_idprof1 only, no idprof2. The BlockedLog PII beacon is real and correctly described as NOT default — gated on isModEnabled('blockedlog') && isALNEQualifiedVersion() && isRegistrationDataSaved().",
        "corrected": "business/dolibarr (Dolibarr 23.0.3, stock dolibarr/dolibarr image) makes an outbound call the operator did not ask for, to https://ping.dolibarr.org/ — the default of MAIN_URL_FOR_PING (htdocs/core/lib/functions2.lib.php:3140). It is on by default and the opt-out is unreachable in this stack.\n\nMECHANISM (matters for egress filtering): the server does not open the socket. main.inc.php:3748 prints a jQuery $.ajax POST into the home page, which the logged-in admin's BROWSER executes; the browser then reports success/failure to the local instance at /core/ajax/pingresult.php, which stores MAIN_FIRST_PING_OK_ID = '<hash> - <version>'. A container-level egress rule will NOT block this.\n\nFIELDS ACTUALLY SENT in 23.0.3: action=dolibarrping, country_code (company country from the ERP's own company record), hash_algo, hash_unique_id, version, version_full, entity, dbtype, php_version, os_version, db_version, distrib, token=notrequired. NOTE: datesys, versionblockedlog and versionblockedlog_full are NOT in 23.0.3 — they exist only on the develop branch (a future release).\n\nNO CSP CHANGE in 23.0.3. The `default-src 'self' ping.dolibarr.org` rewrite at main.inc.php:1648 is develop-only, and is a no-op even there: MAIN_SECURITY_FORCECSP is empty by default, so the rewrite operates on an empty string and the header is never emitted. Dolibarr ships without a CSP header.\n\nNOT A PER-INSTALL ID — the opposite. The official image hardcodes $dolibarr_main_instance_unique_id='myinstanceuniquekey' when DOLI_INSTANCE_UNIQUE_ID is unset (docker-run.sh:85), and this stack never sets it. MAIN_SECURITY_SALT is empty by default, so dol_hash adds no salt. The transmitted hash_unique_id is the fixed constant 8fd1d563da6a3ce1f4b3dda06b90fa285b4bf81b2e55d87de88c1807fc62e945, shared by every Dolibarr Docker deployment that leaves that variable unset. It does not distinguish this install. (conf.php at /var/www/html/conf/conf.php is also not persisted by this compose — it is regenerated on container recreation, to the same constant.) Setting DOLI_INSTANCE_UNIQUE_ID — which is good practice for other reasons, since that value seeds security hashes — would turn this into a genuine per-install identifier.\n\nWHY THE OPT-OUT NEVER APPLIES HERE: install/step2.php:616 renders the \"MakeAnonymousPing\" checkbox pre-checked, and unchecking it only sets a browser cookie (DOLINSTALLNOPING_*). The image sets DOLI_INSTALL_AUTO=1, so the wizard never runs and no cookie is ever set. To actually disable it, set the constant MAIN_FIRST_PING_OK_ID='disabled' (Home > Setup > Other), or point MAIN_URL_FOR_PING elsewhere.\n\nSECOND, NON-DEFAULT BEACON (accurate as claimed): if the BlockedLog module is enabled on an LNE-qualified (French fiscal certification) stable version with registration data saved, action=dolibarrregistration sends the same endpoint the company name, email, idprof1, full postal address, state, zip, town, country plus the IT provider's name, email, phone, address, state, zip, town, country and idprof1 — real business PII. Requires all three gates (isModEnabled('blockedlog'), isALNEQualifiedVersion(), isRegistrationDataSaved()); off by default. The payload sends idprof1 only, not idprof2."
      }
    },
    {
      "stack": "business/invoiceninja",
      "project": "Invoice Ninja",
      "makes_unrequested_call": true,
      "endpoint": "https://pdf.invoicing.co/api/version (daily version check, hardcoded) — plus https://sentry2.invoicing.co (vendor-hosted Sentry, default DSN for crash telemetry)",
      "data_sent": "VERSION CHECK: bare GET, no query string and no body — the server-side job just does file_get_contents(config('ninja.version_url')) and writes the returned string into accounts.latest_version. NO persistent installation ID is transmitted; only source IP + daily timing identify the install. SENTRY: unhandled exception class, message, stack trace and breadcrumbs (log, cache, SQL queries — SQL *bindings* off, HTTP client requests, queue/command info) plus 1% performance traces (traces_sample_rate default 0.01), send_default_pii default false. PERSISTENT INSTALLATION ID: YES on the Sentry path — App\\Exceptions\\Handler sets the Sentry user id to $account->key (the account's stable key) whenever account.report_errors is set; note report_errors only controls the id attachment, not whether the event is sent — the send is gated purely on app()->bound('sentry'), which is true because a vendor DSN ships as the default. Also present but NOT triggered by default: WhiteLabelRenewalService licence renewal call (only fires if a white-label plan was purchased, i.e. operator-initiated) and hosted PDF via pdf.invoicing.co (PDF_GENERATOR default false; this blueprint pins PDF_GENERATOR=snappdf in business/invoiceninja/.env.example:59, so local rendering).",
      "on_by_default": true,
      "disable_by": "VERSION CHECK: no env var exists — 'version_url' => 'https://pdf.invoicing.co/api/version' is hardcoded in config/ninja.php with no env() wrapper; it can only be stopped by not running the scheduler (php artisan schedule:run) or by egress filtering. SENTRY: set SENTRY_LARAVEL_DSN= (empty) in the container environment — config/sentry.php uses config('ninja.sentry_dsn') which is env('SENTRY_LARAVEL_DSN', 'https://…@sentry2.invoicing.co/5'); an empty value leaves 'sentry' unbound and nothing is sent. Neither is currently set in business/invoiceninja/.env.example.",
      "confidence": "source-read",
      "evidence": "https://raw.githubusercontent.com/invoiceninja/invoiceninja/v5-stable/app/Console/Kernel.php:57 — \"$schedule->job(new VersionCheck())->daily();\"; app/Jobs/Util/VersionCheck.php handle() — \"$version_file = trim(@file_get_contents(config('ninja.version_url')));\" then \"Account::query()->whereNotNull('id')->update(['latest_version' => $version_file]);\" under Ninja::isSelfHost(); config/ninja.php:11 \"'version_url' => 'https://pdf.invoicing.co/api/version'\" (no env override), config/ninja.php:45 \"'sentry_dsn' => env('SENTRY_LARAVEL_DSN', \\\"https://39389664f3f14969b4c43dadda00a40b@sentry2.invoicing.co/5\\\")\", config/ninja.php:40 \"'pdf_generator' => env('PDF_GENERATOR', false)\"; config/sentry.php:11 \"'dsn' => config('ninja.sentry_dsn')\", :30 traces_sample_rate default 0.01; app/Exceptions/Handler.php:133-153 self-host branch \"} elseif (app()->bound('sentry')) {\" … scope user id \"auth()->user()->account->key\" … \"Integration::captureUnhandledException($exception);\". Local stack files: /Users/rb3nt/Code/secure-docker-blueprint/business/invoiceninja/.env.example:59 (PDF_GENERATOR=snappdf), no SENTRY_* var present.",
      "verdict": {
        "stack": "business/invoiceninja",
        "refuted": false,
        "reason": "Could not refute — verified line-by-line against upstream tag v5.13.26 (the exact APP_TAG pinned in business/invoiceninja/.env.example:26), not just v5-stable.\n\nVERSION CHECK — confirmed, shipped server, on by default:\n- app/Console/Kernel.php:57 `$schedule->job(new VersionCheck())->daily();` — registered unconditionally, no env/feature guard, no `onOneServer`/`withoutOverlapping`.\n- app/Jobs/Util/VersionCheck.php:47 `$version_file = trim(@file_get_contents(config('ninja.version_url')));` then :50 `Account::query()->whereNotNull('id')->update(['latest_version' => $version_file]);`. Bare GET, no query string, no body, no install ID. Correct that only source IP + daily timing identify the install.\n- config/ninja.php:14 `'version_url' => 'https://pdf.invoicing.co/api/version'` — hardcoded literal, no `env()` wrapper, so no operator override exists. Endpoint is real and live: HTTP 200, 7-byte body `5.13.26`.\n- Not a build/CI/installer/client path. The blueprint's docker-compose.yml:26 runs `supervisord`, and its own comment at :90-91 states supervisor runs php-fpm, queue-worker AND scheduler — so the Laravel scheduler is actually running and the job actually fires.\n- docker-compose.yml:36 sets `NINJA_ENVIRONMENT: selfhost`, so `Ninja::isSelfHost()` is true and the self-host branch is the live one.\n\nSENTRY — confirmed, on by default:\n- config/ninja.php:48 `'sentry_dsn' => env('SENTRY_LARAVEL_DSN', \"https://39389664f3f14969b4c43dadda00a40b@sentry2.invoicing.co/5\")`; config/sentry.php:11 `'dsn' => config('ninja.sentry_dsn')`. Vendor DSN is the fallback, and neither the blueprint (.env.example, docker-compose.yml, ops/entrypoint.sh — grepped, zero SENTRY_* hits) nor upstream's own .env.example sets any SENTRY_* var. So the vendor DSN is what loads. sentry2.invoicing.co resolves (Cloudflare).\n- `sentry/sentry-laravel: ^4` is in composer.json `require` (not require-dev), with `extra.laravel.dont-discover` empty → auto-discovery active.\n- app/Exceptions/Handler.php:133 self-host branch is `} elseif (app()->bound('sentry')) {`; user id set to `auth()->user()->account->key` at :144 only when `account->report_errors` is set (:142); capture at :151-152 is gated on `validException()` && `sentryShouldReport()` — NEITHER consults report_errors. The claim's key structural point is correct: report_errors controls only the ID attachment, not whether the event is sent.\n- Payload defaults confirmed: traces_sample_rate 0.01 (config/sentry.php:30), profiles_sample_rate 0.001 (:33), send_default_pii false (:36), breadcrumbs logs/cache/sql_queries/queue_info/command_info/http_client_requests all default true, sql_bindings default false (:62) and trace sql_bindings false (:89).\n\nNOT-TRIGGERED ITEMS — also confirmed accurate:\n- WhiteLabelRenewalService fires only under `$account->plan == 'white_label'` (VersionCheck.php:63), i.e. operator-purchased.\n- PDF_GENERATOR defaults false (config/ninja.php:43); upstream .env.example ships `hosted_ninja`; the blueprint pins `PDF_GENERATOR=snappdf` at business/invoiceninja/.env.example:59, so PDF rendering is local. That pin is a genuine mitigation and the claim credits it correctly.",
        "corrected": "Claim stands; two mechanism details are misstated and should be tightened, neither weakens it:\n\n1. The outbound version GET is LESS conditional than claimed, not more. VersionCheck.php:47 `file_get_contents()` executes OUTSIDE the `Ninja::isSelfHost()` guard — only the `latest_version` DB write (:49-51) is inside it. The claim's phrasing \"under Ninja::isSelfHost()\" implies the network call is environment-gated; it is not.\n\n2. `app()->bound('sentry')` is NOT true \"because a vendor DSN ships as the default.\" It is true because sentry-laravel's ServiceProvider::register() calls configureAndRegisterClient() unconditionally, which runs `$this->app->alias(HubInterface::class, static::$abstract)` (ServiceProvider.php:387, $abstract='sentry') — Laravel's bound() returns true for aliases, DSN or not. The bind would be true even with an empty DSN. The vendor DSN default is what makes a captured event actually transmit to sentry2.invoicing.co. Same conclusion, different causal chain — worth getting right, because it means clearing SENTRY_LARAVEL_DSN does not make bound() false; it makes the client a no-op transport instead.\n\n3. \"Unhandled exception\" overstates coverage slightly. Sends also pass a message denylist (validException, Handler.php:163-190: file_put_contents, Permission denied, flock, fwrite, LockableFile) and a type denylist (selfHostDontReport: FilePermissionsFailure, MaxAttemptsExceededException, CommandNotFoundException, ValidationException, ModelNotFoundException, NotFoundHttpException, UnableToCreateDirectory, RuntimeException, InvalidArgumentException, CredentialsException, RelationNotFoundException, QueryException). RuntimeException and InvalidArgumentException are broad base classes, so a meaningful share of real faults is filtered. TypeError, ErrorException, LogicException and app-specific exceptions still report. Neither denylist consults report_errors, so the claim's central point survives."
      }
    },
    {
      "stack": "business/openproject",
      "project": "OpenProject Community Edition",
      "makes_unrequested_call": true,
      "endpoint": "https://releases.openproject.com/v1/check.svg",
      "data_sent": "Query string built server-side by SecurityBadgeHelper#security_badge_url: uuid, type (installation type, e.g. 'manual'/'docker'), version (OpenProject semver), db (ActiveRecord adapter name), lang (current user's locale), ee (whether an Enterprise token is active). PERSISTENT INSTALLATION ID: YES — 'uuid' is Setting.installation_uuid, a SecureRandom.uuid generated once and persisted on first read (persist_on_first_read: true), so every check is attributable to the same installation over its whole lifetime. OpenProject's own docs describe the payload as installation type, current version, database type, enterprise status, anonymous unique instance ID and locale, and state no personal information is transmitted. MECHANISM CAVEAT worth stating plainly: the HTTP request is issued by the *admin's browser* — the helper's URL is embedded as <object data=… type=\"image/svg+xml\"> in the administration homescreen block — not by the Rails process. So server-side egress filtering will not suppress it; the setting has to be turned off. It is nonetheless a vendor update check keyed to server-held state (installation UUID, version, DB, EE status), not a generic frontend asset. No other unrequested outbound call found: the 'new features' homescreen block renders local i18n data, and the Enterprise-trial/augur flow is operator-initiated.",
      "on_by_default": true,
      "disable_by": "Administration > System settings > General > uncheck the security badge setting; or configuration flag security_badge_displayed: false (env form OPENPROJECT_SECURITY__BADGE__DISPLAYED=false). The target host itself, security_badge_url, is writable: false — it can only be overridden via configuration file/env, not the UI. Not currently set in /Users/rb3nt/Code/secure-docker-blueprint/business/openproject/.env.example.",
      "confidence": "documented",
      "evidence": "Docs: https://www.openproject.org/docs/system-admin-guide/information/ (security badge matches the version against the official release database; sends installation type, version, database type, enterprise status, anonymous unique instance ID, locale; enabled by default; disable via Administration > System settings > General or security_badge_displayed: false). Source: https://raw.githubusercontent.com/opf/openproject/dev/app/helpers/security_badge_helper.rb — \"info = { uuid: Setting.installation_uuid, type: …installation_type, version: OpenProject::VERSION.to_semver, db: ActiveRecord::Base.connection.adapter_name.downcase, lang: User.current.try(:language), ee: EnterpriseToken.active? }\"; config/constants/settings/definition.rb:1044-1051 \"security_badge_displayed: { default: true }\" and \"security_badge_url: { … default: \\\"https://releases.openproject.com/v1/check.svg\\\", writable: false }\", :684-691 \"installation_uuid: { default: -> { SecureRandom.uuid }, persist_on_first_read: true }\"; spec/helpers/security_badge_helper_spec.rb asserts uri.host == \"releases.openproject.com\" with query keys uuid/type/version/db/lang/ee; app/components/homescreen/blocks/administration.html.erb renders \"content_tag :object, nil, data: security_badge_url, type: \\\"image/svg+xml\\\"\" guarded by display_security_badge_graphic?.",
      "verdict": {
        "stack": "business/openproject",
        "refuted": false,
        "reason": "Verified against the exact pinned version (git tag v17.6.0, matching APP_TAG=17.6.0-slim in /Users/rb3nt/Code/secure-docker-blueprint/business/openproject/.env.example), not only the dev branch. Every element of the claim checks out.\n\nENDPOINT: correct. config/constants/settings/definition.rb:1037-1041 — `security_badge_url: { description: \"URL of the update check badge\", default: \"https://releases.openproject.com/v1/check.svg\", writable: false }`. `writable: false` means it is a vendor-fixed endpoint, not an operator-configured one, so the \"operator's own request in disguise\" escape does not apply.\n\nPAYLOAD: correct, verbatim from app/helpers/security_badge_helper.rb at v17.6.0 — `info = { uuid: Setting.installation_uuid, type: OpenProject::Configuration[:installation_type], version: OpenProject::VERSION.to_semver, db: ActiveRecord::Base.connection.adapter_name.downcase, lang: User.current.try(:language), ee: EnterpriseToken.active? }`, then `uri.query = info.to_query`.\n\nPERSISTENT ID: correct. definition.rb:684-691 — `installation_uuid: { format: :string, default: -> { SecureRandom.uuid }, persist_on_first_read: true, default_by_env: { test: \"test_uuid\" } }`. Persisted on first read, so every check over the install's lifetime is attributable to the same instance.\n\nDEFAULT ON: correct, and stronger than the claim states — the guard is `display_security_badge_graphic?` = `OpenProject::Configuration.security_badge_displayed? && Setting.security_badge_displayed?`. Both resolve to the same definition at definition.rb:1033-1036, commented \"# Display update / security badge, enabled by default\", `security_badge_displayed: { default: true }`. No feature flag, no enterprise gate. The repo's own docker-compose.yml, .env.example and config/entrypoint.sh contain no badge-related override (grep for \"badge\" returns nothing), so the upstream default applies to this deployment as shipped.\n\nSHIPPED SERVER, NOT BUILD/CI/CLIENT: correct. Rendered by the running Rails app in two admin surfaces — app/components/homescreen/blocks/administration.html.erb (`content_tag :object, nil, data: security_badge_url, type: \"image/svg+xml\"` guarded by `display_security_badge_graphic?`) and app/views/admin/info.html.erb:71-72 (same construct). Nothing in docker/prod/* touches it; docker/prod/Dockerfile:40 only sets `ENV OPENPROJECT_INSTALLATION__TYPE=docker`, which means this deployment reports `type=docker` rather than `manual`.\n\nBROWSER-ISSUED MECHANISM: corroborated independently by config/initializers/content_security_policy.rb, which deliberately allowlists the vendor host so the browser load succeeds — line 134 `policy.object_src(OpenProject::Configuration[:security_badge_url])` and line 48 `frame_src << OpenProject::Configuration[:security_badge_url] if OpenProject::Configuration[:security_badge_displayed]`. The app has to open its own CSP for this request, which only makes sense for a browser-issued fetch. Server-side egress filtering on the web/worker containers will therefore not suppress it.\n\nSPEC: spec/helpers/security_badge_helper_spec.rb asserts `uri.host == \"releases.openproject.com\"` and `query.keys` contains exactly uuid/type/version/db/lang/ee.\n\nDOCS: https://www.openproject.org/docs/system-admin-guide/information/ confirms installation type, current version, database type, enterprise status, anonymous unique instance ID and locale; enabled by default; disable at Administration > System settings > General or via `security_badge_displayed: false`.",
        "corrected": "Claim stands. Two precisions worth carrying into any write-up: (1) the badge is embedded on two surfaces, not one — the administration homescreen block AND Administration > Information; both are admin-only, so the request fires when an administrator opens one of those pages, not on every page view and not on a timer. (2) For this repo's deployment the payload carries type=docker (set by docker/prod/Dockerfile:40, OPENPROJECT_INSTALLATION__TYPE=docker), not 'manual'. Operator remedy, since egress filtering cannot help: set OPENPROJECT_SECURITY__BADGE__DISPLAYED=false in business/openproject/.env, or uncheck it in Administration > System settings > General."
      }
    },
    {
      "stack": "business/zammad",
      "project": "Zammad",
      "makes_unrequested_call": true,
      "on_by_default": true,
      "endpoint": "https://images.zammad.com/api/v1/person/image and /api/v1/organization/image (POST); https://geo.zammad.com/calendar and https://geo.zammad.com/lookup?ip=... (GET). All are Zammad Foundation-hosted vendor services.",
      "data_sent": "NO PERSISTENT INSTALLATION ID IS SENT. No version string, no telemetry, no licence check — the payloads carry only the looked-up subject. What does leave the box is third-party PII: (1) Image Service POSTs the PLAINTEXT, lowercased email address of every user as {email: \"...\"} — note this CONTRADICTS Zammad's own privacy page, which claims images are \"discovered using MD5 digests of user email addresses\"; the MD5 is computed server-side at images.zammad.com, so the cleartext address is what crosses the wire. lib/service/image/zammad.rb only skips @example.com. Organization lookups POST the bare email domain. Trigger is app/models/user/avatar.rb: `after_commit :fetch_avatar_for_email, on: %i[create update]` — in a helpdesk every inbound mail from a new sender auto-creates a customer User, so effectively every customer email address that ever writes in is transmitted to Zammad. (2) GeoIP sends end-user session IP addresses as a query parameter. (3) GeoCalendar sends the requesting IP (or nothing) to seed holiday/timezone data at setup. Zammad states no user data is stored/cached on geo.zammad.com, and that no PII is stored on images.zammad.com — that is a retention claim, not a transmission claim.",
      "disable_by": "Admin UI: Settings → System → Services — set \"Image Service\", \"Geo IP Service\", \"Geo Calendar Service\" (and \"Geo Location Service\", which defaults to OpenStreetMap) to the empty \"-\" option. Rails console equivalent: Setting.set('image_backend', ''); Setting.set('geo_ip_backend', ''); Setting.set('geo_calendar_backend', ''); Setting.set('geo_location_backend', ''). IMPORTANT for this blueprint: these are DB-seeded settings with NO environment-variable override, so they cannot be pre-disabled in .env or docker-compose.yml — a fresh stack phones home during first boot/setup before an admin can reach the toggle. The repo's own business/zammad/README.md already records this behaviour observed at runtime.",
      "evidence": "Defaults (all three vendor backends active out of the box) — https://raw.githubusercontent.com/zammad/zammad/develop/db/seeds/settings.rb : line 426 name 'image_backend' / line 443 state: 'Service::Image::Zammad'; line 453 name 'geo_ip_backend' / line 470 state: 'Service::GeoIp::Zammad'; line 507 name 'geo_calendar_backend' / line 524 state: 'Service::GeoCalendar::Zammad'. Endpoints and payloads — https://raw.githubusercontent.com/zammad/zammad/develop/lib/service/image/zammad.rb (API_HOST = 'https://images.zammad.com', UserAgent.post with {email: email}); .../lib/service/geo_ip/zammad.rb (host 'https://geo.zammad.com', \"/lookup?ip=#{CGI.escape address}\"); .../lib/service/geo_calendar/zammad.rb (\"/calendar?ip=...\"). Trigger — https://raw.githubusercontent.com/zammad/zammad/develop/app/models/user/avatar.rb line 8. Vendor documentation — https://docs.zammad.org/en/latest/appendix/privacy.html, section \"External Services\" (\"Zammad utilizes third party web services for certain functions, meaning that user data may occasionally be sent or exposed to third parties\") and https://admin-docs.zammad.org/en/latest/settings/system/services.html. Operator-observed in this repo — /Users/rb3nt/Code/secure-docker-blueprint/business/zammad/README.md, Known Issues: \"geo.zammad.com outbound calls fail silently — init and scheduler attempt to fetch holiday calendar data from https://geo.zammad.com/calendar\". Negative result on version-check/telemetry/licence-check: the complete set of vendor-service integrations is https://api.github.com/repos/zammad/zammad/contents/lib/service (the privacy page itself points to this directory as the source of all third-party integrations) and it contains only geo_calendar, geo_ip, geo_location, image and system_assets — no update or telemetry service. This is a directory-level read, not an exhaustive source audit, so absence of a version check is well-supported but not proven.",
      "confidence": "documented",
      "verdict": {
        "stack": "business/zammad",
        "refuted": false,
        "reason": "Attempted refutation on all five axes; all failed. The claim holds.\n\n1) EVIDENCE READ CORRECTLY. Fetched the raw sources myself.\n- lib/service/image/zammad.rb: `API_HOST = 'https://images.zammad.com'`, `UserAgent.post(\"#{API_HOST}/api/v1/person/image\", { email: email })` after `email.downcase!`, with the only skips being `Rails.env.test? && DISABLE_IN_TEST_ENV` and `email.match?(%r{@example.com$})`. Organization method POSTs `{ domain: domain }` to /api/v1/organization/image after `domain.sub(%r{^.+?@(.+?)$}, '\\1').downcase`, skipping only `example.com`. Plaintext email confirmed — no MD5 anywhere in the file.\n- lib/service/geo_ip/zammad.rb: `host = 'https://geo.zammad.com'`, `url = \"/lookup?ip=#{CGI.escape address}\"`, GET. Early-returns only for 127.0.0.1 and ::1.\n- lib/service/geo_calendar/zammad.rb: same host, `\"/calendar?ip=...\"` or bare `/calendar` when address is nil, GET.\n- The MD5 contradiction is real. https://docs.zammad.org/en/latest/appendix/privacy.html states images are \"discovered using MD5 digests of user email addresses and organization domain names\" — the code sends cleartext. The digest is server-side.\n\n2) SHIPPED SERVER, not a build step or client. The trigger chain is entirely Rails runtime: app/models/user/avatar.rb `after_commit :fetch_avatar_for_email, on: %i[create update]` -> `AvatarCreateJob.perform_later` -> `Avatar.auto_detection(url: user.email)` -> avatar.rb:200 `Service::Image.user(url)`. GeoIP fires from app/controllers/application_controller/has_user.rb:97 (`session[:geo] = Service::GeoIp.location(request.remote_ip)` on session ping whenever remote_ip changes) and from app/models/user_device.rb:38 and :166. GeoCalendar fires from db/seeds.rb:49 (`Calendar.init_setup`, no arg -> bare GET /calendar — this is the init container, exactly matching the operator's observed log) and from app/services/service/user/add_first_admin.rb (`Calendar.init_setup(request.remote_ip)` plus `Service::Image.organization_suggest(user.email)` during the setup wizard).\n\n3) ON BY DEFAULT, and confirmed at the PINNED tag, not just develop. db/seeds/settings.rb ships `state: 'Service::Image::Zammad'`, `state: 'Service::GeoIp::Zammad'`, `state: 'Service::GeoCalendar::Zammad'` as the seeded values. At tag 7.0.1 (the APP_TAG this repo pins in business/zammad/docker-compose.yml) the same three defaults are present at lines 414/441/495, and lib/service/image/zammad.rb at 7.0.1 is byte-identical in host, endpoints and payload keys. No feature flag, no opt-in.\n\n4) NOT THE OPERATOR'S OWN REQUEST. The hosts are hardcoded Ruby constants/literals. The operator configured a helpdesk, an SMTP mailbox and a Traefik host — never images.zammad.com or geo.zammad.com. Behind the stack's nginx + Traefik the real client IP is forwarded, so the 127.0.0.1/::1 guards do not suppress the GeoIP calls.\n\n5) ENDPOINTS CORRECT as written, verbatim.\n\nAlso checked the negative (no telemetry/version/licence check) and it survives. lib/service contains only geo_calendar, geo_ip, geo_location, image, system_assets. UserAgent sends a static `'User-Agent' => 'Zammad User Agent'` header with no version string, and the payloads carry only the looked-up subject — no installation ID. The two other zammad.com hosts in the Ruby tree are both gated and not automatic: lib/ai/provider/zammad_ai.rb (ai.zammad.com) needs a configured token, and app/services/service/beta_ui/send_feedback.rb (beta-ui-feedback.zammad.com) requires `Service::CheckFeatureEnabled.execute(name: 'ui_desktop_beta_switch')` plus an explicit user feedback submission. `packages_token` seeds empty.\n\nThe privacy-vs-transmission distinction the claim draws is correct: Zammad's \"No private images or personally-identifying information are stored on images.zammad.com\" and \"No user information is stored or cached on geo.zammad.com\" are retention statements, and do not bear on what crosses the wire.",
        "corrected": "Claim stands. Three precision notes, none of which change the substance:\n\n(a) The avatar trigger line is fuller than quoted: `after_commit :fetch_avatar_for_email, on: %i[create update], unless: -> { BulkImportInfo.enabled? }`, and `fetch_avatar_for_email` additionally returns early on `Setting.get('import_mode')` and requires `saved_change_to_email?` plus a valid address. So the transmission is scoped to bulk-import-off, non-import-mode user creates and email changes — which in normal helpdesk operation is still every new inbound sender, as claimed.\n\n(b) The cited line numbers (426/443, 453/470, 507/524) are develop-branch positions. At the pinned 7.0.1 tag the same defaults sit at lines 414, 441 and 495. Both were verified; cite the tag when the operator's deployment is what is being described.\n\n(c) There is a FOURTH vendor backend on by default that the claim does not list, and it is not Zammad-hosted: db/seeds/settings.rb ships `geo_location_backend` with `state: 'Service::GeoLocation::Osm'`, and lib/service/geo_location/osm.rb calls `https://nominatim.openstreetmap.org/search?q=%s&format=jsonv2` and `.../reverse?lat=%s&lon=%s&format=jsonv2` — i.e. postal addresses go to OpenStreetMap. The claim's \"all three vendor backends\" phrasing is accurate for Zammad Foundation-hosted endpoints but undercounts the total default outbound surface."
      }
    },
    {
      "stack": "apps/easyappointments",
      "project": "Easy!Appointments",
      "makes_unrequested_call": true,
      "endpoint": "https://easyappointments.org/feed/",
      "data_sent": "Server-side HTTP GET from the PHP container to the vendor's WordPress RSS feed. Payload is the request itself: User-Agent 'Easy!Appointments/<version>' (config('version')), which discloses the EXACT INSTALLED VERSION, plus the server's public source IP. NO PERSISTENT INSTALLATION ID — the vendor sees version + IP only, so it is a coarse install census, not a per-install beacon. Nothing about appointments, customers or the database leaves. Response (10 blog posts) is rendered into the About page; 5s timeout, failures logged and swallowed.",
      "on_by_default": true,
      "disable_by": "",
      "evidence": "https://github.com/alextselegidis/easyappointments/blob/1.6.0/application/controllers/About.php#L91-L114 — fetch_blog_posts(): $rss_url = 'https://easyappointments.org/feed/'; ... 'user_agent' => 'Easy!Appointments/' . config('version'); $rss_content = @file_get_contents($rss_url, false, $context); called unconditionally from About::index() (line 68). Confirmed present at release tag 1.6.0 and on develop; this stack pulls alextselegidis/easyappointments:${APP_TAG} with APP_TAG=latest = 1.6.x per apps/easyappointments/UPSTREAM.md. Hardcoded URL: no constant in config-sample.php, no env var, no UI toggle — only container egress blocking or a code patch stops it. Trigger is an admin opening the backend About page (not periodic/background), but the operator never asked for the call. Separately checked and CLEAN: application/controllers/Update.php runs local DB migrations only, no remote fetch. Google Analytics / Matomo controllers exist but are opt-in, empty by default, and browser-side. Not a full-codebase audit — absence of other calls is not proven.",
      "confidence": "source-read",
      "verdict": {
        "stack": "apps/easyappointments (Easy!Appointments 1.6.0 + MariaDB 11.4)",
        "refuted": false,
        "reason": "Could not refute — every element verified against source AND against the shipped container image.\n\n1) EVIDENCE NOT MISREAD. Fetched application/controllers/About.php at tag 1.6.0 and on develop. Both are byte-identical, 144 lines, and match the quoted snippet verbatim: line 96 `$rss_url = 'https://easyappointments.org/feed/';`, line 100 `'timeout' => 5`, line 101 `'user_agent' => 'Easy!Appointments/' . config('version')`, line 109 `$rss_content = @file_get_contents($rss_url, false, $context);`, failures swallowed via log_message at 112/119/139, loop capped at 10 items (line 126). Line 68 `$blog_posts = $this->fetch_blog_posts();` sits in `index()` with no guard between it and the permission check — unconditional. Response is rendered: application/views/pages/about.php lines 37–48 iterate `vars('blog_posts')` under `lang('latest_blog_posts')`.\n\n2) IT IS THE SHIPPED SERVER, NOT A BUILD STEP. Pulled the actual image this compose file references and grepped inside it. `alextselegidis/easyappointments:1.6.0` → /var/www/html/application/controllers/About.php contains the feed URL and the user_agent line at the same line numbers; /var/www/html/application/config/app.php line 12 = `$config['version'] = '1.6.0';`. Also checked `:latest` (different digest, same content) → same version string, feed URL present. This is a runtime CodeIgniter controller reachable at the `about` route, linked from the backend nav (application/views/components/backend_header.php:107).\n\n3) ON BY DEFAULT, NO TOGGLE. `grep -rn \"easyappointments.org/feed\"` across the whole 1.6.0 tree returns exactly ONE hit — the hardcoded literal. No RSS/feed key anywhere in application/config/. config-sample.php (verified in full) contains only BASE_URL, LANGUAGE, DEBUG_MODE, DB_*, and commented-out GOOGLE_* constants — nothing gating this. No env var, no UI setting.\n\n4) NOT THE OPERATOR'S OWN REQUEST. The URL is a string literal pointing at the vendor's own site. It is not derived from BASE_URL, not from any settings row, not from operator input. Nothing appointment-, customer- or DB-related is in the request; the outbound payload is the GET plus the UA string plus whatever the transport reveals (source IP). No install identifier is generated or sent — the \"coarse census, not per-install beacon\" characterisation is accurate.\n\n5) ENDPOINT CORRECT. https://easyappointments.org/feed/ — verified as written, with verify_peer/verify_peer_name true.\n\nSupporting: CHANGELOG.md for [1.6.0] - 2026-05-27 lists \"Connect RSS updates from the official blog and show them in the about page\". Trigger characterisation is right — About::index() calls `cannot('view', PRIV_USER_SETTINGS)` first and redirects anonymous visitors to login, so this fires only when an authenticated backend user opens the page, not periodically and not for public booking traffic. Update.php was not re-examined here; that part of the claim is untested but is not what the claim rests on.",
        "corrected": "Claim stands. Two supporting details in the write-up are wrong and should be fixed, neither changing the conclusion:\n\n(a) APP_TAG is NOT `latest`. apps/easyappointments/.env.example line reads `APP_TAG=1.6.0` — an explicit pin. (UPSTREAM.md's \"Based on version: `latest` (1.6.x)\" is the stale text that likely caused this.) I verified both tags contain the call, so the pin makes the finding stronger rather than weaker: this stack deploys exactly the version where the code is confirmed present.\n\n(b) The call is NEW IN 1.6.0, not longstanding. About.php at tag 1.5.1 is 79 lines with no fetch_blog_posts, no file_get_contents and no feed URL. This matters operationally: UPSTREAM.md records \"Last verified: 2026-05-03 (1.5.x)\", so the last version anyone actually checked did not have this. It arrived with the 2026-05-27 release and the pin was bumped to 1.6.0 without the behaviour being re-reviewed.\n\nAlso worth stating precisely in any operator-facing note: the version disclosure is the literal string `Easy!Appointments/1.6.0` in the User-Agent, and the only mitigations are container egress blocking or patching application/controllers/About.php — confirmed, since the URL appears exactly once in the tree as a hardcoded literal with no config path to it."
      }
    },
    {
      "stack": "apps/librephotos",
      "project": "LibrePhotos",
      "makes_unrequested_call": true,
      "endpoint": "https://nominatim.openstreetmap.org (geopy Nominatim default endpoint — no custom domain configured, user_agent 'librephotos'). Secondary, first-run only: github.com/LibrePhotos/librephotos-docker releases, huggingface.co/derneuere/librephotos_models, download.pytorch.org, github.com/deepinsight/insightface.",
      "data_sent": "Reverse geocoding: for EVERY scanned photo carrying GPS EXIF, the backend sends that photo's coordinates as 'lat,lon' to the third-party geocoder and stores the returned place name. This is user photo location data leaving the server during a normal library scan. NO PERSISTENT INSTALLATION ID; the third party sees coordinates + server IP + the static 'librephotos' user agent, so a large scan effectively streams the user's location history to a public service. ML model downloads send nothing but the GET (~1.5 GB on first use). AI captioning/LLM is LOCAL — llm.py posts to http://localhost:8008/generate and LLM_MODEL defaults to 'None'; no content goes to a third-party AI provider.",
      "on_by_default": true,
      "disable_by": "FEATURE_REVERSE_GEOCODING=false (env var, librephotos/settings/production.py:73)",
      "evidence": "https://github.com/LibrePhotos/librephotos/blob/dev/apps/backend/librephotos/settings/production.py — line 56 `def _env_flag(name, default=True)`, line 73 `FEATURE_REVERSE_GEOCODING = _env_flag(\"FEATURE_REVERSE_GEOCODING\")` (comment at 67-68: \"They all default to on\"), lines 253-257 `\"MAP_API_PROVIDER\": (os.environ.get(\"MAP_API_PROVIDER\", \"nominatim\"), ...)`. apps/backend/api/geocode/config.py:38-43 — the 'nominatim' provider takes only `\"user_agent\": \"librephotos\"`, no api_key and no custom domain, so geopy uses the public OSM instance (an in-file comment even warns about \"public nominatim under any load\"). apps/backend/api/geocode/geocode.py — `provider = site_config.MAP_API_PROVIDER`, sends f\"{lat},{lon}\"; with a keyed provider and empty MAP_API_KEY it logs \"No API key found for map provider\" and returns {} WITHOUT calling out — nominatim is the one default that needs no key and therefore actually fires out of the box. apps/backend/api/ml_models.py:39-198 lists the runtime model download URLs. /Users/rb3nt/Code/secure-docker-blueprint/apps/librephotos/docker-compose.yml sets neither FEATURE_REVERSE_GEOCODING nor MAP_API_PROVIDER, so upstream defaults apply. Also noted, OUT OF SCOPE (browser-side): MAP_TILE_PROVIDER defaults to \"photoprism\" (production.py:259-263), so the frontend map fetches tiles from PhotoPrism's tile service.",
      "confidence": "source-read",
      "verdict": {
        "stack": "apps/librephotos",
        "refuted": false,
        "reason": "Could not refute — every load-bearing citation checks out against upstream LibrePhotos `dev` and against the image the blueprint actually pins. production.py:56 defines `_env_flag(name, default=True)`; :73 sets FEATURE_REVERSE_GEOCODING from it (comment at 67-68: \"They all default to on\"); :253-257 defaults MAP_API_PROVIDER to \"nominatim\". api/geocode/config.py:38-43 gives the nominatim provider only `user_agent: \"librephotos\"` and a timeout — no `domain` — and geopy's Nominatim falls back to `_DEFAULT_NOMINATIM_DOMAIN = 'nominatim.openstreetmap.org'` over https, so the endpoint in the claim is correct. The call runs in the SHIPPED SERVER on a normal library scan, not in a build step, CI, installer or client: api/directory_watcher/scan_jobs.py:421 `if settings.FEATURE_REVERSE_GEOCODING: AsyncTask(add_geolocation, ...)` -> processing_jobs.py:584 `photo._geolocate()` -> api/models/photo.py:356 `reverse_geocode(new_gps_lat, new_gps_lon)` -> api/geocode/geocode.py:31 `self._geocoder.reverse(f\"{lat},{lon}\")`; also fires on web upload via api/views/upload.py:230. It is ON BY DEFAULT and is not the operator's own configured request: /Users/rb3nt/Code/secure-docker-blueprint/apps/librephotos/docker-compose.yml sets neither FEATURE_REVERSE_GEOCODING nor MAP_API_PROVIDER, and .env.example offers no switch for either, so both upstream defaults apply untouched. The pinned APP_TAG=2026w25 (Docker Hub build 2026-06-21) postdates the \"Remove photon and switch to nominatam as default\" commit (36fb281a, 2025-12-06), so the running image carries this behaviour. No persistent installation ID is sent — the third party sees coordinates, server IP and the static user agent. The secondary model-download endpoints at api/ml_models.py:39-198 match verbatim, and the AI/LLM half of the claim is right in the app's favour: api/llm.py posts only to http://localhost:8008/generate and LLM_MODEL defaults to \"None\".",
        "corrected": "Claim stands; five refinements. (1) The supporting argument that keyed providers short-circuit \"WITHOUT calling out\" is a misread: Geocode.reverse() tests `\"geocode_args\" in self._provider_config`, but get_provider_config() already returns the inner geocode_args dict, so that guard is dead code — and MAP_API_KEY defaults to \"\" not None, so the `is None` test would never fire regardless. Nominatim is still the default that actually goes out, so the conclusion is unaffected. (2) Not literally \"EVERY scanned photo carrying GPS EXIF\" in perpetuity: api/models/photo.py:_geolocate skips the (0,0) null-island fix and skips re-geocoding when coordinates are unchanged AND an album place exists AND the cached geolocation_json[\"_v\"] matches GEOCODE_VERSION. The first full scan of a library does hit every GPS-bearing photo; rescans are cached. (3) The claim omits api/geocode/rate_limit.py (commit 4e8573f2, 2026-05-15), which enforces a 1.1 s minimum between nominatim calls, coordinated across django-q worker processes through a diskcache transaction. The location history leaves as a ~1 req/s drip rather than a burst — throttled, not prevented. (4) The ML model downloads are more default-on than \"first use only\": with the stock defaults CAPTIONING_MODEL=im2txt, TAGGING_MODEL=places365, FACE_RECOGNITION_MODEL=buffalo_sc plus the unconditional batch_calculate_clip_embedding chain, the im2txt, places365, clip-embeddings, download.pytorch.org resnet18-5c106cde.pth and deepinsight buffalo_sc.zip fetches all fire on a default install. (5) Blueprint-specific trap worth telling the operator: .env.example's MAPBOX_API_KEY only seeds constance MAP_API_KEY (production.py:258). It does NOT change MAP_API_PROVIDER, so supplying a Mapbox token for the map view leaves reverse geocoding pointed at the public OSM instance. To stop the outbound call, set FEATURE_REVERSE_GEOCODING=false on the backend service, or set MAP_API_PROVIDER to a keyed provider and supply that key."
      }
    },
    {
      "stack": "apps/opnform",
      "project": "OpnForm",
      "makes_unrequested_call": true,
      "on_by_default": true,
      "endpoint": "https://telemetry.opnform.com/track (OpenPanel, hardcoded client_id d6d85ed5-723f-48de-894e-db2de1a0c7c1 + client_secret sec_236ae00cc86099409ee2). Separate, unrelated: https://api.opnform.com/licenses/validate (LICENSE_API_ENDPOINT) — only fires if the operator enters a licence key, so not unrequested.",
      "data_sent": "PERSISTENT INSTALLATION ID PRESENT — yes. A UUID stored in the settings table (SettingsKey::INSTANCE_ID), cached forever via Cache::rememberForever('telemetry.instance_id'), sent as OpenPanel `profileId`/`instanceId` on every event. Hourly `telemetry:ping` command; the instance only transmits during one hour per day chosen by md5(instanceId), i.e. a per-install daily beacon. Payload: users_count, workspaces_count, forms_count, submissions_count, has_paid_license, app version. Additionally TelemetrySubscriber emits event-level pings (form created, submission, workspace created, user added). Upstream states no PII, no form content, no submission data, no user emails.",
      "disable_by": "Env var OPNFORM_ANONYMOUS_TELEMETRY_DISABLED=true on the api / worker / api-scheduler services (config('telemetry.enabled') = !env('OPNFORM_ANONYMOUS_TELEMETRY_DISABLED', false)). Alternatively unset SELF_HOSTED or set APP_ENV != production, but both break the intended deployment. The blueprint compose currently sets neither — telemetry is live as shipped.",
      "evidence": "https://raw.githubusercontent.com/JhumanJ/OpnForm/main/api/app/Console/Kernel.php (schedule(): `if (config('app.self_hosted')) { ... $schedule->command('telemetry:ping')->hourly(); }`); https://raw.githubusercontent.com/JhumanJ/OpnForm/main/api/config/telemetry.php ('enabled' => !env('OPNFORM_ANONYMOUS_TELEMETRY_DISABLED', false); 'endpoint' => 'https://telemetry.opnform.com/track'); https://raw.githubusercontent.com/JhumanJ/OpnForm/main/api/app/Console/Commands/SendTelemetryPing.php (collectMetrics(): users_count/workspaces_count/forms_count/submissions_count); https://raw.githubusercontent.com/JhumanJ/OpnForm/main/api/app/Service/Telemetry/TelemetryService.php:26-53 (shouldSendTelemetry() = enabled && isProduction && self_hosted; getInstanceId() = Cache::rememberForever(...Setting::get(SettingsKey::INSTANCE_ID))). Blueprint trigger: /Users/rb3nt/Code/secure-docker-blueprint/apps/opnform/docker-compose.yml:166,244,311 set SELF_HOSTED: \"true\"; :124,219,294 set APP_ENV: production; :266-271 run the api-scheduler service with `php artisan schedule:work`.",
      "confidence": "source-read",
      "verdict": {
        "stack": "apps/opnform (OpnForm — Laravel API + Nuxt client, jhumanj/opnform-api:${APP_TAG}, .env.example pins APP_TAG=2.2.2)",
        "refuted": false,
        "reason": "Could not refute — every cited element verifies at the pinned tag v2.2.2 (not just `main`), and it runs in the shipped server, not a build/CI/installer step. api/config/telemetry.php at v2.2.2 is byte-identical to the claim: endpoint https://telemetry.opnform.com/track, hardcoded client_id d6d85ed5-723f-48de-894e-db2de1a0c7c1 and client_secret sec_236ae00cc86099409ee2, 'enabled' => !env('OPNFORM_ANONYMOUS_TELEMETRY_DISABLED', false) — i.e. opt-out, on by default. api/config/app.php has 'self_hosted' => env('SELF_HOSTED', true) — the default is already true; the blueprint additionally sets SELF_HOSTED: \"true\" (docker-compose.yml:166, 244, 311) and APP_ENV: production (:124, :219, :294), so all three gates in TelemetryService::shouldSendTelemetry() (enabled && App::isProduction() && self_hosted) pass. Console/Kernel.php schedules telemetry:ping hourly inside `if (config('app.self_hosted'))`, and the blueprint runs a dedicated api-scheduler with `php artisan schedule:work` (:271), so the schedule is actually executed. SendTelemetryPing gates on md5(instanceId) hour → one beacon per install per day, payload users_count/workspaces_count/forms_count/submissions_count plus has_paid_license. OpenPanelClient::sendRequest() POSTs the payload to the configured endpoint with openpanel-client-id/openpanel-client-secret headers, profileId = instance UUID, on both `identify` and `track`. TelemetrySubscriber subscribes to FormCreated, WorkspaceCreated, UserCreated, FormSubmitted. Not an operator-configured request: nothing in /Users/rb3nt/Code/secure-docker-blueprint/apps/opnform/ mentions telemetry or sets the opt-out variable, and grep over the whole app directory returns zero matches. Egress is available: api and api-worker are on proxy-public, and api-scheduler's app-internal network is deliberately not `internal: true`. Caveat on method: this is source-level verification at git tag v2.2.2 plus the blueprint's compose; I did not unpack the published Docker image or observe live traffic.",
        "corrected": "Accurate version, with three refinements. (1) The first outbound call does not wait for the hourly scheduler: docker/php-fpm-entrypoint runs `php ./artisan telemetry:initialize-instance` on every start of the api container (api/app/Console/Commands/InitializeInstance.php, signature telemetry:initialize-instance). That command mints the UUID via Str::uuid(), stores it as SettingsKey::INSTANCE_ID, and immediately sends an identify + INSTANCE_CREATED event. So the beacon starts at first boot of the api service, before schedule:work ever ticks. (2) SELF_HOSTED is not merely set by the blueprint — env('SELF_HOSTED', true) already defaults to true upstream, so removing the compose line would not stop it; only APP_ENV != production or OPNFORM_ANONYMOUS_TELEMETRY_DISABLED=true does. (3) The transmitted \"app version\" is config('app.docker_version') = env('APP_VERSION_DOCKER'), which the blueprint never sets, so that field ships empty; the counts travel as identify profile properties while the ping event itself carries has_paid_license. Licence sub-claim also verified: config/services.php has 'endpoint' => env('LICENSE_API_ENDPOINT', 'https://api.opnform.com') and LicenseService::checkLicense() returns invalid with no HTTP call when no key is stored — correctly characterised as not unrequested. Separately, apps/opnform/README.md still lists the images as 1.13.2 while .env.example pins APP_TAG=2.2.2 — stale doc, does not affect this finding. Fix for the operator: add OPNFORM_ANONYMOUS_TELEMETRY_DISABLED: \"true\" to the api, api-worker and api-scheduler environment blocks."
      }
    },
    {
      "stack": "apps/vaultwarden",
      "project": "Vaultwarden",
      "makes_unrequested_call": true,
      "endpoint": "https://api.github.com/repos/dani-garcia/vaultwarden/releases/latest, https://api.github.com/repos/dani-garcia/vaultwarden/commits/main, https://api.github.com/repos/dani-garcia/bw_web_builds/releases/latest, https://github.com/dani-garcia/vaultwarden (connectivity ping), https://cloudflare.com/cdn-cgi/trace (time check). Separately, opt-in push relay: https://push.bitwarden.com + https://identity.bitwarden.com (EU: api.bitwarden.eu / identity.bitwarden.eu).",
      "data_sent": "Version check: plain server-side GETs, no request body, no query params, NO PERSISTENT INSTALLATION ID — the vendor/GitHub sees only source IP, TLS/HTTP headers and request timing; release tags and commit SHA are compared locally in the server and rendered into the diagnostics page (the JS only reads server-embedded JSON, so this is server egress, not browser egress). Not periodic and not at startup: it fires only when an admin loads /admin/diagnostics, cached 600s. Push relay is the one path that DOES carry A PERSISTENT INSTALLATION ID (PUSH_INSTALLATION_ID + PUSH_INSTALLATION_KEY, registered at bitwarden.com/host) — but only if the operator opts in. Not reported: ICON_SERVICE default `internal` makes the server fetch favicons from the domains stored in users' vaults — third-party hosts, but driven by user-entered data, so out of scope here.",
      "on_by_default": true,
      "disable_by": "No dedicated toggle for the diagnostics version check — it is reachable only via the admin panel, so it stops if the admin page is never opened, or if the admin panel is disabled entirely (leave ADMIN_TOKEN unset / DISABLE_ADMIN_TOKEN=true). Push relay: PUSH_ENABLED, default false upstream and pinned false in this blueprint (apps/vaultwarden/docker-compose.yml: `PUSH_ENABLED: ${VW_PUSH_ENABLED:-false}`).",
      "evidence": "https://raw.githubusercontent.com/dani-garcia/vaultwarden/main/src/api/admin.rs — get_release_info() (three api.github.com URLs), has_http_access() pings github.com, get_ntp_time() queries cloudflare.com/cdn-cgi/trace; all behind the /admin/diagnostics handler with a 600s cache and the comment that GitHub allows 60 requests/hour. https://raw.githubusercontent.com/dani-garcia/vaultwarden/main/src/static/scripts/admin_diagnostics.js — reads latest_release from server-embedded diagnostics_json, confirming the fetch is server-side. https://raw.githubusercontent.com/dani-garcia/vaultwarden/main/.env.template — ICON_SERVICE default internal, PUSH_ENABLED default false, PUSH_RELAY_URI default https://push.bitwarden.com. Local: /Users/rb3nt/Code/secure-docker-blueprint/apps/vaultwarden/docker-compose.yml (push vars default empty/false).",
      "confidence": "source-read",
      "verdict": {
        "stack": "Rust / Rocket (Vaultwarden server), deployed via Docker Compose",
        "refuted": false,
        "reason": "Verified against the shipped server source (full tarball of dani-garcia/vaultwarden main), not just the cited raw files. All five version-check URLs exist verbatim in src/api/admin.rs: has_http_access() line 637 (HEAD https://github.com/dani-garcia/vaultwarden), get_release_info() lines 655/661/670 (the three api.github.com endpoints), get_ntp_time() line 683 (https://cloudflare.com/cdn-cgi/trace). The #[cached(ttl = 600, sync_writes = \"default\")] attribute and the \"Github only allows 60 requests per hour, and we use 3 here already\" comment are exactly as claimed.\n\nRUNS IN SHIPPED SERVER, NOT A BUILD STEP: these are Rust reqwest calls through make_http_request in the running binary. Confirmed server-side, not browser: admin_diagnostics.js line 427 does JSON.parse(document.getElementById(\"diagnostics_json\").textContent) and initVersionCheck reads dj.latest_release from that embedded blob; every browser fetch() in that file targets ${BASE_URL} (the operator's own server: /api/config, /webauthn-connector.html, /admin/does-not-exist, /admin/diagnostics/http?code=NNN).\n\nONLY CALLER IS THE DIAGNOSTICS HANDLER: a repo-wide grep of src/ found the three functions called nowhere except lines 726, 738 and 779 inside #[get(\"/diagnostics\")]. No startup hook, no scheduled/periodic job. The only other github.com string in the Rust source (src/api/core/mod.rs:235) is a literal inside the /api/config JSON response, not a request target.\n\nNO INSTALLATION ID ON THE VERSION CHECK: src/http_client.rs:37 sets a static User-Agent \"Vaultwarden\" with no version and no identifier. URLs carry no query params; requests are GET/HEAD with no body. So the claim's \"source IP, TLS/HTTP headers, timing only\" is accurate.\n\nPUSH RELAY IS GENUINELY OPT-IN: src/config.rs:529 push_enabled defaults false; push_installation_id/push_installation_key default to empty String. Every call site in src/api/push.rs (lines 88, 139, 268) returns early on !CONFIG.push_enabled(). Defaults https://push.bitwarden.com (line 531) and https://identity.bitwarden.com (line 533) are only reached after opt-in. Local /Users/rb3nt/Code/secure-docker-blueprint/apps/vaultwarden/docker-compose.yml sets PUSH_ENABLED: ${VW_PUSH_ENABLED:-false} with ID/key/URIs defaulting empty — off unless the operator acts. ICON_SERVICE default \"internal\" confirmed at src/config.rs:681.\n\nNOT THE OPERATOR'S OWN CONFIGURED REQUEST: the five version-check hosts are hardcoded, not derived from any operator setting.\n\nTwo precisions that do not contradict the claim: (1) the admin panel is disabled unless ADMIN_TOKEN or DISABLE_ADMIN_TOKEN is set (src/config.rs:1019-1024, 1268) — this blueprint does set VW_ADMIN_TOKEN, so the page is reachable here; (2) there is no dedicated off-switch for the version check, but make_http_request calls should_block_host(), so HTTP_REQUEST_BLOCK_REGEX can block github.com/cloudflare.com. The claim's own scoping (\"not periodic and not at startup: it fires only when an admin loads /admin/diagnostics\") is precise and should be kept — dropping it would overstate the behaviour."
      }
    },
    {
      "stack": "business/listmonk",
      "project": "Listmonk",
      "makes_unrequested_call": true,
      "endpoint": "https://update.listmonk.app/update.json",
      "data_sent": "Plain HTTP GET with no query params and no body. NO PERSISTENT INSTALLATION ID and no instance UUID is sent — the running version string is only used locally to compare against the fetched JSON. What the vendor endpoint therefore observes is source IP, Go's default user agent and a recurring 24h heartbeat, which still fingerprints the instance by IP even without an ID. Result surfaces as an update banner in the admin UI.",
      "on_by_default": true,
      "disable_by": "Setting key `app.check_updates` — admin UI: Settings > General > \"Check for updates\" (toggle off). Default is 'true', seeded in schema.sql.",
      "evidence": "https://raw.githubusercontent.com/knadh/listmonk/master/cmd/updates.go — updateCheckURL = \"https://update.listmonk.app/update.json\", fetched with http.Get, first run ~15 min after startup then on the interval. https://raw.githubusercontent.com/knadh/listmonk/master/cmd/main.go — `go app.checkUpdates(versionString, time.Hour*24)` gated on app.check_updates. https://raw.githubusercontent.com/knadh/listmonk/master/schema.sql — ('app.check_updates', 'true') is the seeded default.",
      "confidence": "source-read",
      "verdict": {
        "stack": "business/listmonk",
        "refuted": false,
        "reason": "Could not refute — the claim is accurate on every checked point, verified against upstream master.\n\n1) Evidence supports it, not misread. cmd/updates.go:13 `const updateCheckURL = \"https://update.listmonk.app/update.json\"`. Line 44 is `resp, err := http.Get(updateCheckURL)` — bare GET against a compile-time constant URL: no query string built, no body, no headers set, no client with a custom UserAgent. So Go's default `Go-http-client/…` UA and the source IP are what the endpoint sees. `curVersion` is confirmed local-only: it is passed in at line 39, stripped by regex at line 41, and used solely in `semver.Compare(v, curVersion)` at line 71. No install ID or instance UUID exists anywhere in the request path. Timing matches: `time.Sleep(time.Minute * 15)` then `fnCheck()` (lines 84-85), then `time.NewTicker(interval)` loop (88-93).\n\n2) It runs in the shipped server, not a build step, CI, or installer. cmd/main.go:304-305 `if ko.Bool(\"app.check_updates\") { go app.checkUpdates(versionString, time.Hour*24) }` sits in `main()` between the `App{}` struct literal and `initHTTPServer(...)` — i.e. the normal serve path of the same binary the operator runs. The 24h interval is confirmed there.\n\n3) On by default. schema.sql:249 seeds `('app.check_updates', 'true')`. The gate reads koanf, and cmd/main.go:186 calls `initSettings(q.Query, db, ko)`, which (cmd/init.go:434-456) unmarshals the DB settings row and merges it into `ko` via `confmap.Provider` — so the seeded `true` is what line 304 reads on a fresh install. No feature flag or opt-in required.\n\n4) Not the operator's own configured request. The URL is a hardcoded constant, not derived from any operator-set field (unlike, say, root_url or an SMTP host).\n\n5) Endpoint is correct as quoted, including the `/update.json` path.\n\nApplies to this repo's deployment: business/listmonk/docker-compose.yml:49 uses the official `listmonk/listmonk:${APP_TAG}` image and sets no LISTMONK_app__check_updates override.\n\nTwo nuances worth carrying into the write-up, neither of which refutes anything:\n- The response type (updates.go:25-31) also carries a `Messages[]` array with title/description/url/priority, rendered alongside the update prompt. So the endpoint is a vendor-controlled announcement channel into the admin UI, not strictly a version banner — slightly broader than the claim states, not narrower.\n- The opt-out is the admin UI setting (DB), not an env var. Env is loaded at main.go:125 but DB settings are merged over it at line 186, so `LISTMONK_app__check_updates=false` is expected to be overwritten by the DB value. (Inferred from koanf last-load-wins merge order, not runtime-tested.) The 15-minute initial sleep is explicitly documented upstream as the window for an admin to disable it before any request leaves the host — meaning a container restarted more often than every 15 min would never call out."
      }
    }
  ],
  "refuted": [
    {
      "stack": "apps/caldiy",
      "project": "Cal.diY (community fork of Cal.com)",
      "makes_unrequested_call": true,
      "endpoint": "https://t.calendso.com — Jitsu collector operated by Cal.com, Inc., with hardcoded server-to-server write key \"s2s.2pvs2bbpqq1zxna97wcml.esb6cikfrf7yn0qoh1nj1\".",
      "data_sent": "Page/usage events emitted from the Next.js server side, containing: page_url, url, title, referrer, queryString, doc_encoding, utc_time, ipAddress, isTeamBooking, isAuthorized, onVercel. NO PERSISTENT INSTALLATION ID is generated in the telemetry module — there is no instance UUID or licence identifier in the payload, so this is per-request usage tracking rather than a per-install beacon. It is still meaningful egress: ipAddress plus full booking URLs/titles goes to a Cal.com-hosted collector, and booking page URLs can carry attendee-identifying slugs. No licence/registration call and no CALCOM_LICENSE_KEY variable appear in cal.diy's .env.example (EE licensing appears to be stripped from the MIT community edition); no default-on AI integration found — Intercom, PostHog, Sentry, Formbricks and lingo.dev are all present as integrations but all default to empty/unset, i.e. off unless the operator supplies a key.",
      "disable_by": "CALCOM_TELEMETRY_DISABLED=1 (upstream comment: \"Set this to '1' if you don't want Cal to collect anonymous usage\"); setting it to \"1\" makes the Jitsu driver config undefined. This blueprint ALREADY sets it: apps/caldiy/docker-compose.yml line 164 and apps/caldiy/docker-compose.local.yml line 78 — so telemetry is off as shipped here, but on in a stock deployment.",
      "on_by_default": true,
      "confidence": "documented",
      "evidence": "https://raw.githubusercontent.com/calcom/cal.diy/main/.env.example — CALCOM_TELEMETRY_DISABLED is present but EMPTY/unset by default, with the comment \"Set this to '1' if you don't want Cal to collect anonymous usage\" (opt-out, therefore on by default). Endpoint and payload from https://raw.githubusercontent.com/calcom/cal.diy/main/packages/lib/telemetry.ts — jitsu host \"https://t.calendso.com\", key \"s2s.2pvs2bbpqq1zxna97wcml.esb6cikfrf7yn0qoh1nj1\", fields page_url/doc_encoding/url/title/ipAddress/queryString/isTeamBooking/referrer/onVercel/isAuthorized/utc_time, gated by process.env.CALCOM_TELEMETRY_DISABLED === \"1\". Blueprint mitigation: /Users/rb3nt/Code/secure-docker-blueprint/apps/caldiy/docker-compose.yml:164 `CALCOM_TELEMETRY_DISABLED: \"1\"`. Caveat: I could not fetch apps/web/middleware.ts (HTTP 404 on that path) to confirm exactly which routes wire collectEvents in this fork, so the route coverage is inferred from the telemetry module and the upstream env comment, not read directly.",
      "verdict": {
        "stack": "apps/caldiy — Cal.diY (community MIT fork of Cal.com), shipped as ghcr.io/rubennati/cal.diy:v6.2.0-4",
        "refuted": true,
        "reason": "REFUTED — the code exists but is unreachable dead code; no outbound call is made.\n\nThe claim's static reading of packages/lib/telemetry.ts is accurate: the Jitsu host \"https://t.calendso.com\", the key \"s2s.2pvs2bbpqq1zxna97wcml.esb6cikfrf7yn0qoh1nj1\", the field names, and the guard process.env.CALCOM_TELEMETRY_DISABLED === \"1\" are all really there, and .env.example really does ship CALCOM_TELEMETRY_DISABLED empty with an opt-out comment. But \"on by default\" does not follow, because nothing ever executes that config.\n\nVerified by whole-tree grep of the upstream source (154 MB tarball of calcom/cal.diy@main), not just code search:\n\n1. next-collect — the library that exports collectEvents and CollectOpts, and the ONLY thing that would ever POST to the Jitsu server — does not appear anywhere in the tree. Not in any package.json, never imported.\n2. nextCollectBasicSettings and extendEventData appear ONLY in their own definition file (packages/lib/telemetry.ts:39 and :68). Zero consumers.\n3. The string \"@calcom/lib/telemetry\" is imported by ZERO files. The module is fully orphaned. (Grep hits for \"lib/telemetry\" are a different module, @calcom/features/watchlist/lib/telemetry, a Sentry span helper.)\n4. apps/web contains NO middleware.ts anywhere (find returns empty). The upstream Cal.com next-collect middleware wiring that ran this per-request does not exist in this fork. The claimant's HTTP 404 on that path WAS the finding — they recorded it as a failed fetch and inferred route coverage anyway.\n5. All five surviving telemetry.event(...) call sites are commented out: login-view.tsx:155, UserProfile.tsx:84, useSubmitPersonalOnboarding.ts:89, EventTypeWebWrapper.tsx:357 (line comments), and UserSettings.tsx:49-51 (a /* */ block around the whole useEffect). telemetryEventTypes is not even imported in UserSettings.tsx — it survives only inside the comment.\n6. \"t.calendso.com\", the write key, and the string \"jitsu\" occur nowhere in the repository except that one orphaned file.\n7. CollectOpts (telemetry.ts:39) is an undefined type — never imported, never declared anywhere in the tree. A live, type-checked module could not reference it. This is vestigial code left behind when the community edition stripped next-collect.\n\nTwo payload misreads independently weaken the claim even as a hypothetical. extendEventData hardcodes title: \"\", ipAddress: \"\", queryString: \"\", referrer: \"\" — empty string literals. The claim read field NAMES as field VALUES, so its central harm argument (\"ipAddress plus full booking URLs/titles goes to a Cal.com-hosted collector\") is unsupported; IP, title, referrer and query string would be blank even if the module ran. Separately, collectPageParameters reads document.location/document.characterSet — browser-side, not \"emitted from the Next.js server side\" as claimed.\n\nChecks requested: (a) evidence was misread — the file contents are real but the wiring was inferred, not read; (b) it runs in neither the shipped server nor a build step nor CI — it runs nowhere; (c) not on by default, because it is not on at all; (d) not an operator-configured request; (e) the endpoint string is correct but nothing dials it.\n\nScope caveat: I verified upstream calcom/cal.diy@main, which apps/caldiy/UPSTREAM.md states the rubennati fork's main mirrors 1:1; the blueprint ships the fork's release branch. I did not decompile the shipped image. Since the fork's documented divergence is CI/security-review only and the code is orphaned upstream, the conclusion holds.\n\nThe blueprint's CALCOM_TELEMETRY_DISABLED: \"1\" (apps/caldiy/docker-compose.yml:164) is correct and harmless to keep as belt-and-braces against a future upstream rewiring, but it is not currently mitigating live egress. It should not be described to operators as blocking a phone-home that occurs.",
        "corrected": "Cal.diY carries a vestigial telemetry module (packages/lib/telemetry.ts) inherited from Cal.com that still contains a hardcoded Jitsu endpoint (https://t.calendso.com) and server-to-server write key. It is dead code: the next-collect library that would transmit the events is not a dependency, the exported config objects have no consumers, apps/web has no middleware.ts, and every remaining telemetry.event() call site is commented out. Cal.diY makes no outbound telemetry call to Cal.com, Inc. by default or otherwise. The .env.example does ship CALCOM_TELEMETRY_DISABLED empty with opt-out wording, which is misleading but inert. The blueprint sets CALCOM_TELEMETRY_DISABLED=\"1\" (apps/caldiy/docker-compose.yml:164); this is reasonable defence-in-depth against a future upstream rewiring, not mitigation of current egress. Worth tracking on upstream sync — if next-collect ever returns, this becomes live opt-out telemetry. Separately confirmed: no licence/registration call, no CALCOM_LICENSE_KEY, and Intercom/PostHog/Sentry/Formbricks/lingo.dev all default to empty (off)."
      }
    }
  ],
  "clean": [
    {
      "stack": "apps/heimdall",
      "project": "Heimdall (lscr.io/linuxserver/heimdall)",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "https://appslist.heimdall.site/ — GET list.json and files/<sha>.zip. Reached only on explicit user action, never on boot or a schedule.",
      "data_sent": "Plain unauthenticated GETs for a catalogue file and per-app zip archives. No payload body, no query parameters, and NO PERSISTENT INSTALLATION ID — SupportedApps.php builds the URLs purely from config('app.appsource') plus a per-app SHA, so the request carries nothing that identifies the instance beyond its source IP and User-Agent. No telemetry, licence, registration or AI endpoint exists anywhere in the app.",
      "disable_by": "APP_SOURCE environment variable — config/app.php: \"'appsource' => env('APP_SOURCE', 'https://appslist.heimdall.site/')\". Point it at an internal mirror, or simply never click the app-list refresh, since nothing calls it on its own.",
      "evidence": "https://raw.githubusercontent.com/linuxserver/Heimdall/master/app/SupportedApps.php — only two static URLs, both built from config('app.appsource'): \"$list_url = config('app.appsource').'list.json';\" in getList() and \"$zipurl = config('app.appsource').'files/'.$app->sha.'.zip';\" in getFiles(). https://raw.githubusercontent.com/linuxserver/Heimdall/master/config/app.php — the default value. Trigger path: https://raw.githubusercontent.com/linuxserver/Heimdall/master/app/Http/Controllers/ItemController.php — getList() is reached only via checkAppList(), an explicit route that dispatches ProcessApps; getFiles() is reached only via Application::getApp() (https://raw.githubusercontent.com/linuxserver/Heimdall/master/app/Application.php) when a user adds an app whose bundled files are missing or stale. Nothing automatic: https://raw.githubusercontent.com/linuxserver/Heimdall/master/app/Console/Kernel.php has an empty schedule() (only a commented-out 'inspire' example), and the image ships no scheduler and no cron — https://api.github.com/repos/linuxserver/docker-heimdall/git/trees/master?recursive=1 shows only init-heimdall-config and svc-queue (a Laravel queue worker that runs jobs a user dispatched). Heimdall never checks for a new Heimdall version. Local stack: /Users/rb3nt/Code/secure-docker-blueprint/apps/heimdall/docker-compose.yml sets no APP_SOURCE override; ALLOW_INTERNAL_REQUESTS=false only governs user-configured widget probes, which are out of scope.",
      "confidence": "source-read"
    },
    {
      "stack": "apps/unifi",
      "project": "UniFi Network Application (Ubiquiti, lscr.io/linuxserver/unifi-network-application)",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "Not established. Documented egress from Ubiquiti's own Required Ports Reference is TCP 8883 \"Remote Access service\" and UDP 123 NTP; no hostname is given for either. Ubiquiti's firmware hosts dl.ui.com and fw-download.ubnt.com appear only in a manual-upgrade context. NOTE the build-time-only call I confirmed and excluded: the LSIO Dockerfile queries https://fw-update.ubnt.com/api/firmware-latest?filter=eq~~product~~unifi-controller... to pick the version to bake in — that runs on LinuxServer's build hosts, not in the shipped image, so it is out of scope.",
      "data_sent": "Not established, therefore the presence of a PERSISTENT INSTALLATION ID could not be confirmed either way. I could not determine what, if anything, the controller sends on its own. Ubiquiti's privacy policy does assert broad automatic collection — \"We may collect certain information about your devices, your network, your system and third party devices connected to your network or system when you use the Services ('Usage Data') ... device identifiers\" — but it does not say which product, which endpoint, or whether it is on by default, so it is not usable as evidence of a specific unrequested call.",
      "disable_by": "Remote/cloud access is opt-in and gated on signing in with a UI Account (Ubiquiti's self-hosting guide labels remote management \"(Optional)\"), so it is not an unrequested call. Update behaviour lives at Settings > Control Plane > Updates, where release channel and auto-update can be configured — but the vendor does not document the default, and the LSIO image ships no environment variable or system.properties key for it.",
      "evidence": "NOT-FOUND, and absence of a hit is NOT evidence of absence here — the controller is closed-source Java, so method step 2 (reading source) is unavailable, and Ubiquiti's docs never state the default. What I actually checked: https://help.ui.com/api/v2/help_center/en-us/articles/218506997.json (Required Ports Reference) lists egress TCP 8883 \"Remote Access service\" and UDP 123 \"NTP (time sync)\" with no hostnames and no default stated; https://help.ui.com/api/v2/help_center/en-us/articles/34210126298775.json (Self-Hosting UniFi) marks the UI Account / remote management step \"(Optional)\"; https://help.ui.com/api/v2/help_center/en-us/articles/7605005245975.json (UniFi Updates) documents Settings > Control Plane > Updates but states no default and no endpoint; https://help.ui.com/api/v2/help_center/en-us/articles/204910064.json (Advanced Updating Techniques) names only https://dl.ui.com/unifi/firmware/ and https://fw-download.ubnt.com/data/ for manual, user-initiated firmware downloads. Image side: https://raw.githubusercontent.com/linuxserver/docker-unifi-network-application/main/Dockerfile — the fw-update.ubnt.com calls are build-time only; https://raw.githubusercontent.com/linuxserver/docker-unifi-network-application/main/root/defaults/system.properties contains only port and MongoDB settings, no update/cloud/analytics keys; https://api.github.com/repos/linuxserver/docker-unifi-network-application/git/trees/main?recursive=1 shows no runtime script that calls out. help.ui.com blocks direct page fetches (HTTP 403) and web.archive.org is unreachable from here, so the Zendesk JSON API above was the only vendor channel available. A packet capture of a running controller is the way to settle this; documentation cannot. Local stack for context: /Users/rb3nt/Code/secure-docker-blueprint/apps/unifi/docker-compose.yml (app is on proxy-public, so it has egress) and /Users/rb3nt/Code/secure-docker-blueprint/apps/unifi/UPSTREAM.md, which already claims \"Self-hosted (local controller) has no cloud dependency\" — that claim is unverified by anything I could cite and should not be strengthened on my account.",
      "confidence": "not-found"
    },
    {
      "stack": "apps/seafile",
      "project": "Seafile Community Edition 13",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "",
      "data_sent": "Nothing. No version check, no telemetry, no licence call, no vendor relay found in the server-side code paths. NO PERSISTENT INSTALLATION ID exists anywhere in the server, because no beacon exists to carry one. The only auto-update mechanism in the whole Seafile org is in the DESKTOP CLIENT (haiwen/seafile-client:src/auto-update-service.cpp, Sparkle appcast at https://www.seafile.com/api/client-updates/...), which does not run in this stack and is out of scope. Push notifications are served by the self-hosted notification-server container (apps/seafile/notification-server.yml), not by a vendor relay; the string \"notification.seafile.com\" in haiwen/seafile:daemon/notif-mgr.c is a WebSocket subprotocol NAME in the client daemon, not a host. AI is absent from CE: upstream defaults are ENABLE_SEAFILE_AI = False, ENABLE_AI_CHAT = False, ENABLE_FACE_RECOGNITION = False, SEAFILE_AI_SERVER_URL = '' and this stack ships no AI service. Only optional-and-off upstream egress is SEAFILE_SERVER_LETSENCRYPT (ACME to Let's Encrypt), unused here because TLS terminates at Traefik. Caveat: seaf-server/seahub were read selectively, not exhaustively.",
      "disable_by": "",
      "evidence": "https://raw.githubusercontent.com/haiwen/seafile-docker/master/scripts/scripts_13.0/start.py and .../bootstrap.py (13.0 container start path: no urllib/requests/curl/wget, no URL, no update check); https://raw.githubusercontent.com/haiwen/seahub/master/seahub/settings.py (ENABLE_SEAFILE_AI = False, ENABLE_AI_CHAT = False, SEAFILE_AI_SERVER_URL = ''); `gh search code --owner haiwen \"seafile.com\"` returns only README/manual links, UI templates (seahub/templates/download.html, frontend/src/components/dialog/about-dialog.js — browser-side links a user clicks) and the desktop-client Sparkle updater; local stack config contacts internal hosts only (/Users/rb3nt/Code/secure-docker-blueprint/apps/seafile/config/seahub_custom.py:14 METADATA_SERVER_URL = 'http://seafile-md-server:8084')",
      "confidence": "source-read"
    },
    {
      "stack": "apps/seafile-pro",
      "project": "Seafile Professional Edition 13",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "",
      "data_sent": "Nothing unrequested. LICENCE: validated entirely offline — seahub/utils/licenseparse.py opens the local settings.LICENSE_PATH (seafile-license.txt in the data volume) and splits key=value lines; no URL, no activation call, no registration. The admin system-info endpoint (seahub/api2/endpoints/admin/sysinfo.py) calls parse_license() locally and makes no HTTP request. NO PERSISTENT INSTALLATION ID is transmitted anywhere — the licence hash stays on disk. No version check and no telemetry found (same code-search result as CE). AI (in-scope category 5) is present as a component but OFF: seafile-ai.yml is deliberately NOT in the default COMPOSE_FILE (.env.example:22) and ENABLE_SEAFILE_AI=false (.env.example:76); upstream seahub default is ENABLE_SEAFILE_AI = False. If an operator turns it on, apps/seafile-pro/seafile-ai.yml defaults to SEAFILE_AI_LLM_TYPE=openai / SEAFILE_AI_LLM_MODEL=gpt-4o-mini, but SEAFILE_AI_LLM_URL and SEAFILE_AI_LLM_KEY are empty — content can only reach a third-party LLM after the operator supplies a key, i.e. explicitly requested. Face embedding, SeaSearch, Elasticsearch, SeaDoc, md/thumbnail/notification servers all run in-stack. SECONDARY NOTE, judged in-scope-adjacent rather than unrequested: clamav.yml IS in the default COMPOSE_FILE, so the clamav/clamav container's freshclam updater fetches virus definitions from Cisco/ClamAV mirrors (current.cvd.clamav.net / database.clamav.net) on its own schedule — a definition/version fetch inherent to running an AV the operator chose to enable, carrying no install ID and no Seafile data.",
      "disable_by": "AI: leave ENABLE_SEAFILE_AI=false and keep seafile-ai.yml out of COMPOSE_FILE (both already the default in apps/seafile-pro/.env.example). ClamAV signature fetch: drop clamav.yml from COMPOSE_FILE.",
      "evidence": "https://raw.githubusercontent.com/haiwen/seahub/master/seahub/utils/licenseparse.py (local file read only, no URL); https://raw.githubusercontent.com/haiwen/seahub/master/seahub/api2/endpoints/admin/sysinfo.py (parse_license(), no network); https://manual.seafile.com/13.0/setup/setup_pro_by_docker/ (licence = seafile-license.txt placed in the volume, read at start); /Users/rb3nt/Code/secure-docker-blueprint/apps/seafile-pro/.env.example:18,22,76 (seafile-ai.yml excluded from COMPOSE_FILE, ENABLE_SEAFILE_AI=false); /Users/rb3nt/Code/secure-docker-blueprint/apps/seafile-pro/seafile-ai.yml:22-25 (SEAFILE_AI_LLM_URL and SEAFILE_AI_LLM_KEY empty by default); `gh search code --owner haiwen \"seafile.com\"` shows no server-side vendor endpoint",
      "confidence": "source-read"
    },
    {
      "stack": "business/vikunja",
      "project": "Vikunja",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "",
      "data_sent": "Nothing by default. NO PERSISTENT INSTALLATION ID and no beacon of any kind exists in the codebase — there is no update-check, licence-check or usage-statistics feature to carry one. The only vendor endpoints in the product are opt-in and default OFF: Sentry error tracking (api DSN https://440eedc957d545a795c17bbaf477497c@o1047380.ingest.sentry.io/4504254983634944, frontend DSN .../6024480) and Unsplash backgrounds (needs an operator-supplied access token). Gravatar is not contacted by default: the default avatar provider is 'initials'. Everything else that leaves the box is operator- or user-initiated and therefore out of scope: Todoist/Trello/Microsoft-To-Do migrations, user-created webhooks, S3/Typesense/OIDC endpoints the operator configures.",
      "disable_by": "Already off. Keys are VIKUNJA_SENTRY_ENABLED=false (sentry.enabled), VIKUNJA_SENTRY_FRONTENDENABLED=false (sentry.frontendenabled), VIKUNJA_METRICS_ENABLED=false, VIKUNJA_BACKGROUNDS_PROVIDERS_UNSPLASH_ENABLED=false.",
      "confidence": "source-read",
      "evidence": "https://vikunja.io/docs/config-options/ documents sentry.enabled=false, sentry.frontendenabled=false, metrics.enabled=false, backgrounds.providers.unsplash.enabled=false. Source read at shallow clone of github.com/go-vikunja/vikunja@main: pkg/config/config.go:378-379 sets only the two Sentry DSN defaults — there is no SentryEnabled.setDefault(...) anywhere (grep 'SentryEnabled' returns only the key definition at config.go:77 and the four GetBool() guards in pkg/events/events.go:99, pkg/modules/migration/handler/listeners.go:80, pkg/routes/routes.go:226,253), so the Go zero value false applies. pkg/config/config.go:485 DefaultSettingsAvatarProvider.setDefault(\"initials\"). config-raw.json (the upstream config reference, 63 KB) contains no update/telemetry/statistics key at all. grep for checkForUpdate|check_for_update|api.github.com|dl.vikunja.io|newVersion across pkg/ and frontend/src/ returned zero hits, and a full inventory of hardcoded https:// hosts in pkg/**/*.go turned up only licence headers, docs links and test fixtures — no client call to a vendor host."
    },
    {
      "stack": "business/opensign",
      "project": "OpenSign",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "https://sso.opensignlabs.com/api (SSO login path only); https://app.opensignlabs.com/api/app (newsletter, opt-in, default unchecked)",
      "data_sent": "Nothing by default, and NO PERSISTENT INSTALLATION ID is generated or transmitted anywhere in the server source. Two conditional vendor paths exist: (1) newsletter — if the admin ticks 'Subscribe to OpenSign newsletter' during first-admin creation the SERVER posts {Name, Email, Domain} (the instance's own hostname) to https://app.opensignlabs.com/api/app/classes/Newsletter; the checkbox is initialised unchecked. (2) SSO — the Parse auth adapter 'sso' validates a bearer token against https://sso.opensignlabs.com/api/oauth/userinfo, i.e. a vendor-hosted relay, but it only fires on an explicit sso-provider login and the OSS frontend has no SSO login path. RED FLAG for future pulls: apps/OpenSignServer/package.json:53 declares posthog-node ^5.21.2, yet the package is imported nowhere in the repository source — no analytics call exists in the shipped code path today, but the blueprint pins the rolling `main` tag, so this can become live on any pull. Out of scope but worth knowing: outgoing signature emails embed https://qikinnovation.ams3.digitaloceanspaces.com/logo.png, fetched by the recipient's mail client, not by the server.",
      "disable_by": "Newsletter: leave the signup checkbox unticked (its default). SSO relay: set SSO_API_URL to your own endpoint (auth/authadapter.js:4 falls back to the vendor URL). No env var exists to pin/disable posthog-node should it ever be wired up — pin the image by digest instead of the `main` tag.",
      "confidence": "source-read",
      "evidence": "https://docs.opensignlabs.com/docs/self-host/docker/run-locally/ documents 24 env vars and none for telemetry, update checks or licensing. Source read at shallow clone of github.com/OpenSignLabs/OpenSign@main: a full inventory of hardcoded https:// hosts under apps/OpenSignServer/**/*.{js,cjs,mjs} (node_modules excluded) yields only qikinnovation.ams3.digitaloceanspaces.com (email logo), sso.opensignlabs.com (auth/authadapter.js:4, `process.env.SSO_API_URL || 'https://sso.opensignlabs.com/api'`, registered at index.js:161 `auth: { google: {...}, sso: SSOAuth }`), app.opensignlabs.com (cloud/parsefunction/Newsletter.js:9), and googleapis.com (operator-configured Gmail OAuth). apps/OpenSign/src/pages/AddAdmin.jsx:34 `const [isSubscribeNews, setIsSubscribeNews] = useState(false)` with the call gated at lines 130-131 proves the newsletter post is opt-in and default OFF. grep for posthog|mixpanel|segment.io|amplitude|telemetry|analytics|openai|anthropic|api.github.com|/releases|checkUpdate across the whole repo excluding node_modules and package-lock.json hits only apps/OpenSignServer/package.json:53. Caveat: this is the source of the `main` branch; the published opensign/opensignserver:main image is not semver-tagged and was not itself disassembled."
    },
    {
      "stack": "apps/adminer",
      "project": "Adminer",
      "makes_unrequested_call": false,
      "endpoint": "https://www.adminer.org/version/?current=<version> — contacted by the ADMIN'S BROWSER, not by the container",
      "data_sent": "A version check exists and is on by default, but the self-hosted server never makes it: the page injects an iframe pointing at adminer.org, the browser fetches it, and the result is POSTed back to Adminer (script=version) and cached in a temp file. adminer.org therefore sees the admin's browser IP/User-Agent and the installed version in ?current=. NO PERSISTENT INSTALLATION ID. The PHP process makes no outbound HTTP call — every file_get_contents in the checked files reads local paths (design.inc.php:39 reads the cached version file). Because the request originates in the browser, blocking the container's egress does NOT stop it.",
      "on_by_default": true,
      "disable_by": "",
      "evidence": "https://raw.githubusercontent.com/vrana/adminer/v4.8.1/adminer/static/functions.js — `function verifyVersion(current, url, token) { cookie('adminer_version=0', 1); var iframe = document.createElement('iframe'); iframe.src = 'https://www.adminer.org/version/?current=' + current;` then `ajax(url + 'script=version', ..., event.data + '&token=' + token)`. adminer/include/design.inc.php:57 fires it on page load whenever the adminer_version cookie is absent (the cookie suppresses it for 1 day); design.inc.php:137 whitelists `\"frame-src\" => \"https://www.adminer.org\"` in Adminer's own CSP, confirming the request is browser-side by design. No env var or config toggle in the official adminer image (compose sets only ADMINER_DEFAULT_SERVER / ADMINER_DESIGN); suppressing it needs a custom build or browser-side blocking. Reported false because the task scopes out anything only the browser does — but the operator should know an admin session pings the vendor. www.adminer.org/en/privacy/ returns 404, so there is no vendor privacy page to cite.",
      "confidence": "source-read"
    },
    {
      "stack": "apps/it-tools",
      "project": "IT-Tools",
      "makes_unrequested_call": false,
      "endpoint": "",
      "data_sent": "Nothing. The shipped image is nginx serving a static SPA build (no application runtime that could call out at all). The repo does carry plausible-tracker ^0.3.8, but it is inert unless enabled at BUILD time, and the upstream Dockerfile enables nothing — so the published ghcr image ships with the tracker off. If someone rebuilt with the env vars set, the traffic would still be browser-to-Plausible, never server-side. No installation ID anywhere.",
      "on_by_default": false,
      "disable_by": "already off — VITE_TRACKER_ENABLED (default false), VITE_PLAUSIBLE_DOMAIN and VITE_PLAUSIBLE_API_HOST (both default '')",
      "evidence": "https://raw.githubusercontent.com/CorentinTh/it-tools/main/src/config.ts — `isTrackerEnabled: { ... default: false, env: 'VITE_TRACKER_ENABLED' }`, domain and apiHost default ''. package.json lists plausible-tracker ^0.3.8 and no Sentry/PostHog/Matomo/GA. https://raw.githubusercontent.com/CorentinTh/it-tools/main/Dockerfile — build stage runs plain `pnpm build` with no VITE_* build args, production stage is `nginx:stable-alpine` serving /app/dist, so nothing in the container can originate a request. index.html contains no third-party script tags. Not a full-codebase audit — absence of other calls is not proven, but a static-file container has no server-side egress path.",
      "confidence": "source-read"
    },
    {
      "stack": "apps/paperless-ngx",
      "project": "Paperless-ngx",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "https://api.github.com/repos/paperless-ngx/paperless-ngx/releases/latest — requested by the BACKEND (httpx, RemoteVersionView), not the browser, but only when the UI setting is on",
      "data_sent": "NO PERSISTENT INSTALLATION ID. The server sends a plain GET with only `Accept: application/json` — no version, no instance ID, no query parameters; it just reads `tag_name` from the response and compares locally (result cached 15 min). GitHub sees the server's IP and the default httpx User-Agent, nothing more.",
      "disable_by": "Nothing to disable — it is off unless a user turns it on. Per-user UI toggle: Settings > General > Update checking (frontend key `general-settings:update-checking:enabled`, default false). The legacy env var PAPERLESS_ENABLE_UPDATE_CHECK is deprecated since v1.9.2 and is not set in the blueprint; a one-time migration copies it into the frontend setting if it was ever set.",
      "evidence": "https://raw.githubusercontent.com/paperless-ngx/paperless-ngx/dev/src/documents/views.py:4077-4107 (class RemoteVersionView: httpx.get(\"https://api.github.com/repos/paperless-ngx/paperless-ngx/releases/latest\", headers={\"Accept\": \"application/json\"})); https://raw.githubusercontent.com/paperless-ngx/paperless-ngx/dev/src/paperless/urls.py (re_path(\"^remote_version/\", RemoteVersionView.as_view(), name=\"remoteversion\")); https://raw.githubusercontent.com/paperless-ngx/paperless-ngx/dev/src-ui/src/app/data/ui-settings.ts:201-204 (UPDATE_CHECKING_ENABLED, type 'boolean', default: false); https://docs.paperless-ngx.com/configuration/ on PAPERLESS_ENABLE_UPDATE_CHECK: \"This setting was deprecated in favor of a frontend setting after v1.9.2.\" No PAPERLESS_ENABLE_UPDATE_CHECK in /Users/rb3nt/Code/secure-docker-blueprint/apps/paperless-ngx/docker-compose.yml.",
      "confidence": "documented"
    },
    {
      "stack": "apps/monicahq",
      "project": "Monica",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "",
      "data_sent": "No self-initiated payload identified, therefore no installation ID observed. (Older Monica 2.x/3.x lines carried a CHECK_VERSION ping to version.monicahq.com that sent an instance UUID; no such variable or command exists in the current main/5.x tree that this stack pins.)",
      "disable_by": "",
      "evidence": "https://raw.githubusercontent.com/monicahq/monica/main/.env.example — the project's own env reference contains no version-check, telemetry, instance-UUID or Sentry variable. https://raw.githubusercontent.com/monicahq/monica/main/config/monica.php — only APP_VERSION/APP_COMMIT, no version-check URL. https://raw.githubusercontent.com/monicahq/monica/main/routes/console.php — scheduled tasks are model:prune, queue:prune-batches, queue:prune-failed, telescope:prune, UpdateAddressBooks, ProcessScheduledContactReminders, CleanSyncToken, CleanLogs; none contacts monicahq.com. A repo-tree scan for telemetry/version-check filenames returned only tests/Feature/ZiggyVersionCheckTest.php (a local JS-route version assertion, no network). Stack pins monica:5-apache per /Users/rb3nt/Code/secure-docker-blueprint/apps/monicahq/UPSTREAM.md:10. Caveat: this is the absence of a hit in the env reference, config, scheduler and filename scan — it is not a full audit of the 2860-file tree, so absence is not proof. UpdateAddressBooks syncs CardDAV servers the user configured (out of scope).",
      "confidence": "source-read"
    },
    {
      "stack": "apps/photoview",
      "project": "Photoview",
      "makes_unrequested_call": false,
      "on_by_default": false,
      "endpoint": "",
      "data_sent": "No self-initiated payload identified, therefore no installation ID observed. The only third-party host in the project's env reference is Mapbox, and it is opt-in (MAPBOX_TOKEN is commented out by default) and used by the browser map view — out of scope, and not set in the blueprint compose.",
      "disable_by": "",
      "evidence": "https://raw.githubusercontent.com/photoview/photoview/master/api/example.env — the project's own env reference lists only DB driver/URL, listen IP/port, media cache, SERVE_UI, MAPBOX_TOKEN (commented out), DEVELOPMENT_MODE, VIDEO_HARDWARE_ACCELERATION; no telemetry, update-check or analytics variable. https://raw.githubusercontent.com/photoview/photoview/master/README.md — no mention of telemetry, analytics, usage statistics, update check or phone-home. A scan of the 700-entry repo tree found no file named for telemetry/analytics/update-check/instance-id. Caveat: this is documentation silence plus a filename scan, not a read of every Go source file — absence of a hit is not evidence of absence. Note the blueprint deliberately dropped the upstream Watchtower service (/Users/rb3nt/Code/secure-docker-blueprint/apps/photoview/UPSTREAM.md:28), which would otherwise have polled a registry.",
      "confidence": "not-found"
    },
    {
      "stack": "business/kimai",
      "project": "Kimai",
      "makes_unrequested_call": false,
      "endpoint": "",
      "data_sent": "No egress path established. No persistent installation ID found, because no phone-home path was found at all. Kimai's documented update procedure is operator-driven (pull a new image / run migrations); nothing in the documentation describes an automatic version lookup, usage statistics, licence check or third-party AI call from the server.",
      "on_by_default": false,
      "disable_by": "",
      "evidence": "https://www.kimai.org/documentation/configurations.html — the settings/configuration reference documents timesheet, invoicing, auth, calendar and kiosk options and is SILENT on update checks, telemetry, usage statistics, licence validation and third-party AI. Local /Users/rb3nt/Code/secure-docker-blueprint/business/kimai/docker-compose.yml has no update/telemetry-related env vars. This is documentation silence plus a prior source search that returned no telemetry strings — absence of a hit is NOT proof of absence; the container was not run and its traffic not observed, so this is unestablished rather than confirmed clean.",
      "confidence": "not-found"
    }
  ]
}
