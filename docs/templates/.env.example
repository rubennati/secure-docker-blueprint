# =============================================
# {App Name} – Environment
# =============================================
# Copy this file to .env and adjust all values.
# NEVER commit the .env file.
# =============================================

# --- Images ---
APP_IMAGE=your-image:1.0.0
DB_IMAGE=mariadb:11.4

# --- Container ---
CONTAINER_NAME_APP=myapp-app
CONTAINER_NAME_DB=myapp-db

# --- General ---
TIMEZONE=Europe/Vienna
COMPOSE_PROJECT_NAME=myapp

# --- Database ---
DB_MYSQL_DATABASE=myapp
DB_MYSQL_USER=myapp_user

# --- App Configuration ---
# (add app-specific variables here)

# --- Traefik Routing ---
APP_TRAEFIK_HOST=myapp.example.com
APP_TRAEFIK_CERT_RESOLVER=cloudflare-dns
# TLS profile: tls-basic (TLS 1.2), tls-aplus (TLS 1.2 strict), tls-modern (TLS 1.3 only)
APP_TRAEFIK_TLS_OPTION=tls-basic
# Access: acc-public (unrestricted), acc-tailscale (VPN only)
APP_TRAEFIK_ACCESS=acc-public
# Security: sec-0 (none), sec-1 (basic headers), sec-2 (standard), sec-3 (strict), sec-4 (strict + rate limit)
APP_TRAEFIK_SECURITY=sec-2
APP_INTERNAL_PORT=80

# --- Secrets ---
# Create the secrets directory and files:
#   mkdir -p secrets
#   openssl rand -base64 32 > secrets/db_pwd.txt
#   openssl rand -base64 32 > secrets/db_root_pwd.txt
