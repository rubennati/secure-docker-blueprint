# Dolibarr — local
#
#   cp .env.local.example .env.local

# --- Images ------------------------------------------------------- [image] --
APP_TAG=23.0.3
DB_TAG=11.4

# --- Database -------------------------------------------------------- [app] --
DB_NAME=dolibarr
DB_USER=dolibarr
# openssl rand -hex 32
DB_PASSWORD=__REPLACE_ME__
# openssl rand -hex 32
DB_ROOT_PASSWORD=__REPLACE_ME__

# --- First-install admin --------------------------------------------- [app] --
DOLI_ADMIN_LOGIN=admin
# openssl rand -base64 24 | tr -d '\n'
DOLI_ADMIN_PASSWORD=__REPLACE_ME__

# --- Timezone -------------------------------------------------------- [app] --
TZ=UTC
