# Zammad — local
#
#   cp .env.local.example .env.local

# --- Images ------------------------------------------------------- [image] --
APP_TAG=7.1.2-0013
DB_TAG=16-alpine
REDIS_TAG=7.4-alpine
MEMCACHED_TAG=1.6.41

# --- Database -------------------------------------------------------- [app] --
DB_NAME=zammad_production
DB_USER=zammad
#   openssl rand -base64 32 | tr -d '\n'
DB_PASSWORD=__REPLACE_ME__

# --- Timezone -------------------------------------------------------- [app] --
TZ=UTC
