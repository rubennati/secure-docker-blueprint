# /cron runs every scheduled job for any caller, without a login. The
# facturascripts-cron service runs the same jobs from the command line.
<LocationMatch "(?i)^/cron$">
    Require all denied
</LocationMatch>
