# vLLM — local env (CPU build)
#   cp .env.local.example .env.local

APP_TAG=v0.29.0
VLLM_UID=1000
VLLM_GID=1000
# Small enough to start on a CPU-only machine.
VLLM_MODEL=Qwen/Qwen2.5-0.5B-Instruct
# openssl rand -hex 24
VLLM_API_KEY=__REPLACE_ME__
TZ=UTC
