# =============================================
# Lychee – Environment
# =============================================
# Copy this file to .env and adjust all values.
# NEVER commit the .env file.
# =============================================

COMPOSE_PROJECT_NAME=lychee

# --- Domain & Traefik ---
APP_TRAEFIK_HOST=gallery.example.com
APP_TRAEFIK_CERT_RESOLVER=cloudflare-dns
APP_TRAEFIK_TLS_OPTION=tls-aplus
APP_TRAEFIK_ACCESS=acc-public
APP_TRAEFIK_SECURITY=sec-2
APP_INTERNAL_PORT=80
TRAEFIK_NETWORK=proxy-public

# --- Images ---
# lycheeorg/lychee (https://hub.docker.com/r/lycheeorg/lychee)
APP_TAG=v7.5.4
# mariadb (https://hub.docker.com/_/mariadb)
DB_TAG=11.4
# redis (https://hub.docker.com/_/redis)
REDIS_TAG=7.4-alpine

# --- Containers ---
CONTAINER_NAME_APP=${COMPOSE_PROJECT_NAME}-app
CONTAINER_NAME_DB=${COMPOSE_PROJECT_NAME}-db
CONTAINER_NAME_REDIS=${COMPOSE_PROJECT_NAME}-redis

# --- User Mapping ---
# Must match the UID/GID that owns ./volumes/* on the host.
PUID=1000
PGID=1000

# --- Database ---
DB_NAME=lychee
DB_USER=lychee

# --- App Key ---
# Laravel APP_KEY — encrypts sessions, cookies, and password-reset tokens.
# Generate once with:
#   docker run --rm lycheeorg/lychee:${APP_TAG} php artisan key:generate --show
# Keep the "base64:" prefix in the output. Rotating invalidates sessions.
APP_KEY=__REPLACE_ME__

# --- Timezone ---
# Examples: UTC, Europe/Berlin, Europe/London, America/New_York
TZ=UTC
