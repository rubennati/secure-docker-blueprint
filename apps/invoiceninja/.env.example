# =============================================
# Invoice Ninja – Environment
# =============================================
# Copy this file to .env and adjust all values.
# NEVER commit the .env file.
# =============================================

# --- Images ---
APP_IMAGE=invoiceninja/invoiceninja:5.13.16
DB_IMAGE=mysql:8.4
NGINX_IMAGE=nginx:1.27-alpine

# --- Container ---
CONTAINER_NAME_APP=invoiceninja-app
CONTAINER_NAME_DB=invoiceninja-db
CONTAINER_NAME_NGINX=invoiceninja-nginx

# --- General ---
TIMEZONE=Europe/Vienna
COMPOSE_PROJECT_NAME=invoiceninja

# --- Database ---
DB_MYSQL_DATABASE=invoiceninja
DB_MYSQL_USER=in_user

# --- App Configuration ---
# Generate with: php artisan key:generate --show
IN_APP_KEY=__REPLACE_ME__
IN_USER_EMAIL=admin@example.com

# --- Traefik Routing ---
APP_TRAEFIK_HOST=invoice.example.com
APP_TRAEFIK_CERT_RESOLVER=cloudflare-dns
# TLS profile: tls-basic (TLS 1.2), tls-aplus (TLS 1.2 strict), tls-modern (TLS 1.3 only)
APP_TRAEFIK_TLS_OPTION=tls-basic
# Access: acc-public (unrestricted), acc-tailscale (VPN only)
APP_TRAEFIK_ACCESS=acc-tailscale
# Security: sec-0 (none), sec-1 (basic headers), sec-2 (standard), sec-3 (strict), sec-4 (strict + rate limit)
APP_TRAEFIK_SECURITY=sec-3
APP_INTERNAL_PORT=80

# --- Secrets ---
# Create the secrets directory and files:
#   mkdir -p secrets
#   openssl rand -base64 32 | tr -d '\n' > secrets/db_pwd.txt
#   openssl rand -base64 32 | tr -d '\n' > secrets/db_root_pwd.txt
#   openssl rand -base64 32 | tr -d '\n' > secrets/in_user_pwd.txt
