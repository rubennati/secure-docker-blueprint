# =============================================
# OpnForm – Environment
# =============================================
# Copy this file to .env and adjust all values.
# NEVER commit the .env file.
# =============================================

COMPOSE_PROJECT_NAME=opnform

# --- Domain & Traefik ---
APP_TRAEFIK_HOST=forms.example.com
APP_TRAEFIK_CERT_RESOLVER=cloudflare-dns
APP_TRAEFIK_TLS_OPTION=tls-aplus
APP_TRAEFIK_ACCESS=acc-public
APP_TRAEFIK_SECURITY=sec-2
TRAEFIK_NETWORK=proxy-public

# --- Images ---
# jhumanj/opnform-api + jhumanj/opnform-client
# (https://github.com/JhumanJ/OpnForm/pkgs/container/opnform)
APP_TAG=1.13.2
# nginx (https://hub.docker.com/_/nginx)
NGINX_TAG=1
# Maximum upload size passed to nginx
NGINX_MAX_BODY_SIZE=64m
# postgres (https://hub.docker.com/_/postgres)
DB_TAG=16-alpine
# redis (https://hub.docker.com/_/redis)
REDIS_TAG=7.4-alpine

# --- Containers ---
CONTAINER_NAME_NGINX=${COMPOSE_PROJECT_NAME}-nginx
CONTAINER_NAME_API=${COMPOSE_PROJECT_NAME}-api
CONTAINER_NAME_API_WORKER=${COMPOSE_PROJECT_NAME}-api-worker
CONTAINER_NAME_API_SCHEDULER=${COMPOSE_PROJECT_NAME}-api-scheduler
CONTAINER_NAME_UI=${COMPOSE_PROJECT_NAME}-ui
CONTAINER_NAME_DB=${COMPOSE_PROJECT_NAME}-db
CONTAINER_NAME_REDIS=${COMPOSE_PROJECT_NAME}-redis

# --- Database ---
DB_NAME=opnform
DB_USER=opnform
# Laravel reads DB_PASSWORD from env only (no _FILE support).
# Value must match contents of .secrets/db_pwd.txt.
DB_PWD_INLINE=__REPLACE_ME__

# --- App Keys & Secrets ---
# APP_KEY — Laravel encryption key (sessions, cookies, tokens).
# The opnform-api entrypoint waits for DB before running artisan, so
# key:generate hangs when run standalone. Generate directly instead:
#   echo "base64:$(openssl rand -base64 32)"
APP_KEY=__REPLACE_ME__

# JWT_SECRET — signs API JWT tokens.
#   openssl rand -base64 32
JWT_SECRET=__REPLACE_ME__

# FRONT_API_SECRET — shared secret between the Laravel API and the Nuxt UI
# for authenticated SSR requests. Must be the same value in both.
#   openssl rand -base64 32
FRONT_API_SECRET=__REPLACE_ME__

# --- Mail ---
# "log"  = write emails to the Laravel log (dev / test)
# "smtp" = use the MAIL_HOST/PORT/USER/PASS settings below (prod)
MAIL_MAILER=log
MAIL_FROM_ADDRESS=forms@example.com
MAIL_FROM_NAME=OpnForm

# Only used when MAIL_MAILER=smtp
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
# tls / ssl / null
MAIL_ENCRYPTION=tls

# --- Timezone ---
# Examples: UTC, Europe/Berlin, Europe/London, America/New_York
TZ=UTC
