# Easy!Appointments — local
#
#   cp .env.local.example .env.local

# --- Images ------------------------------------------------------- [image] --
APP_TAG=1.6.0
DB_TAG=11.4

# --- Database -------------------------------------------------------- [app] --
DB_NAME=easyappointments
DB_USER=easyappointments
# openssl rand -base64 32 | tr -d '\n'
DB_PASSWORD=__REPLACE_ME__
# openssl rand -base64 32 | tr -d '\n'
DB_ROOT_PASSWORD=__REPLACE_ME__

# --- Timezone -------------------------------------------------------- [app] --
TZ=UTC
