# Windmill — local env
#   cp .env.local.example .env.local

DB_TAG=18.6
DB_NAME=windmill
DB_USER=postgres
# Hex, because it is embedded in DATABASE_URL: openssl rand -hex 32
DB_PASSWORD=__REPLACE_ME__
APP_TAG=1.814.0
WM_UID=1000
WM_GID=1000
TZ=UTC
