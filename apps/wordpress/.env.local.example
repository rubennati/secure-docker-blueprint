# WordPress — local
#
#   cp .env.local.example .env.local

# --- Images ------------------------------------------------------- [image] --
APP_TAG=7.0.2-php8.3-apache
DB_TAG=11.4

# --- Database -------------------------------------------------------- [app] --
DB_NAME=wordpress
DB_USER=wp_user
# openssl rand -base64 32 | tr -d '\n'
DB_PASSWORD=__REPLACE_ME__
DB_ROOT_PASSWORD=__REPLACE_ME__

# --- App Configuration ------------------------------------------------ [app] --
WP_TABLE_PREFIX=wp_

# --- Timezone -------------------------------------------------------- [app] --
TZ=UTC
