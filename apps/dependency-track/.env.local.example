# Dependency-Track — local
#
#   cp .env.local.example .env.local

# --- Images ---
APP_TAG=5.1.0
DB_TAG=16-alpine

# --- Database ---
DB_NAME=dtrack
DB_USER=dtrack
# openssl rand -hex 32
DB_PASSWORD=__REPLACE_ME__

# --- App Configuration ---
# openssl rand -base64 32 | tr -d '\n'
DT_KEK=__REPLACE_ME__

# --- Timezone ---
TZ=UTC
