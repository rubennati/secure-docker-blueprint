# Ghost — local
#
#   cp .env.local.example .env.local

# --- Images ------------------------------------------------------- [image] --
APP_TAG=6.54.0-alpine
DB_TAG=8.4

# --- Database -------------------------------------------------------- [app] --
DB_NAME=ghost
DB_USER=ghost_user
# openssl rand -hex 32
DB_PASSWORD=__REPLACE_ME__
# openssl rand -hex 32
DB_ROOT_PASSWORD=__REPLACE_ME__

# --- Timezone -------------------------------------------------------- [app] --
TZ=UTC
